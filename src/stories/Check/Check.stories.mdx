import { Canvas, Meta, Story, Source, ArgsTable } from '@storybook/addon-docs'
import { action } from '@storybook/addon-actions'

import VvCheck from '../../components/VvCheck/VvCheck.vue'

<Meta
	title="Components/Check"
	argTypes={{
		onClick: { action: 'click', table: { disable: true } },
		onChange: { action: 'change', table: { disable: true } },
		onFocus: { action: 'focus', table: { disable: true } },
		onBlur: { action: 'blur', table: { disable: true } }
	}}
	component={VvCheck}
/>

export const Template = (args, { argTypes }) => ({
	props: Object.keys(argTypes),
	components: { VvCheck },
	setup() {
		return { args }
	},
	data: () => ({ rdValue: args.modelValue || [] }),
	template:
		/*html*/
		`
		<div class="flex flex-row justify-between">
			<vv-check v-bind="args" v-model:modelValue="rdValue"/>
			<strong v-if="rdValue">Valore selez: {{rdValue}} </strong> 
		</div>
        `
})

# VvCheck

<Source language="jsx" code={'<vv-check/>'} />

<Canvas>
	<Story
		name="vv-check"
		args={{
			id: 1,
			label: 'opzione 1',
			name: 'ck1',
			disabled: false,
			switch: false,
			binary: false,
			modelValue: [],
			value: { a: 1 }
		}}
		argTypes={{
			id: {
				description: 'Id input element.',
				table: { category: 'Attributes' }
			},
			name: {
				description: 'Nome input element',
				table: { category: 'Attributes' }
			},
			value: {
				description: 'Valore input element',
				table: { category: 'Attributes' }
			}
		}}>
		{Template.bind({})}
	</Story>
</Canvas>

<ArgsTable story="vv-check" />
