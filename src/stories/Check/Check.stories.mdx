import { Canvas, Meta, Story, Source } from '@storybook/addon-docs'
import { action } from '@storybook/addon-actions'

import VvCheck from '../../components/VvCheck/VvCheck.vue'

<Meta
	title="Components/Check"
	args={{
		modelValue: []
	}}
	component={VvCheck}
/>

export const Template = (args, { argTypes }) => ({
	props: Object.keys(argTypes),
	components: { VvCheck },
	setup() {
		return { args }
	},
	data: () => ({ ckValue: [] }),
	template:
		/*html*/
		`
		<div class="flex flex-row justify-between">
			<vv-check v-bind="args" v-model:modelValue="ckValue"/>
			<strong v-if="ckValue">Valore selez: {{ckValue}} </strong> 
		</div>
        `
})

<Source language="jsx" code={'<vv-check/>'} />

<Canvas>
	<Story
		name="vv-check"
		args={{
			id: 1,
			label: 'opzione 1',
			name: 'ck1',
			disabled: false,
			switch: false,
			binary: false,
			modelValue: [],
			value: 1
		}}
		argTypes={{
			id: {
				description: 'Id input element.',
				table: { category: 'Attributes' }
			},
			name: {
				description: 'Nome input element',
				table: { category: 'Attributes' }
			},
			value: {
				description: 'Valore input element',
				table: { category: 'Attributes' }
			}
		}}>
		{Template.bind({})}
	</Story>
</Canvas>

<ArgsTable story="vv-check" />
