import { Canvas, Meta, Story, Source } from '@storybook/addon-docs'
import VvDropdown from '../../components/VvDropdown/VvDropdown.vue'

<Meta title="Components/Dropdown/Props/maxValues" />

# VvDropdown maxValues

Let's define a story for our multple `max-values` dropdown.

<Canvas>
	<Story name="maxValues">
		{() => {
			return {
				components: { VvDropdown },
				data() {
					return {
						value: '',
						options: ['Option 4', 'Option 5', 'Option 6']
					}
				},
				template:
					/*html*/
					`<div class="w-1/2">
						<vv-dropdown v-model:modelValue="value" :options="options" label="Label 1" multiple max-values="2" />
						value: {{ value }}
					</div>`
			}
		}}
	</Story>
</Canvas>

<Source
	language="html"
	/*html*/
	code={`<vv-dropdown v-model:modelValue="value" :options="options" label="Label 1" multiple max-values="2" />`}
/>
