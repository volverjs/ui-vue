import { Canvas, Meta, Story, Source } from '@storybook/addon-docs'
import VvDropdown from '../../components/VvDropdown/VvDropdown.vue'
import { Template } from './Dropdown.stories.mdx'

<Meta title="Components/Dropdown/Props/labelNoResults" />

# VvDropdown maxValues

Let's define a story for our multple `max-values` dropdown.

<Canvas>
	<Story
		name="labelNoResults"
		args={{
			options: [],
			labelNoResults: 'Non sono presenti elementi!'
		}}>
		{Template.bind({})}
	</Story>
</Canvas>

<Source
	language="html"
	code={`<vv-dropdown v-model:modelValue="value" :options="options" label-no-results="Non sono presenti elementi!" />`}
/>
