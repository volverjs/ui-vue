import { Canvas, Meta, Story, Source } from '@storybook/addon-docs'
import VvDropdown from '../../components/VvDropdown/VvDropdown.vue'

<Meta title="Components/Dropdown/Props/searchable" />

# VvDropdown searchable

Let's define a story for our select with props:

`searchable` `label-no-result` `search-placeholder` and `debounce-search`

<Source
	language="json"
	/*html*/
	code={`[
	{
		"label": "Option 1",
		"value": 1
	},
	{
		"label": "Option 2",
		"value": 2
	},
	{
		"label": "Option 3",
		"value": 3
	}
]`}
/>

<Canvas>
	<Story name="searchable">
		{() => {
			return {
				components: { VvDropdown },
				data() {
					return {
						value: '',
						searchText: '',
						options: [
							{
								label: 'Option 1',
								value: 1
							},
							{
								label: 'Option 2',
								value: 2
							},
							{
								label: 'Option 3',
								value: 3
							}
						]
					}
				},
				template:
					/*html*/
					`<div class="w-1/2 flex gap-32">
						<vv-dropdown v-model:modelValue="value" @change:search="searchText = $event" :options="options" label="Label 1" searchable label-no-result="No results" search-placeholder="Search" debounce-search="1000" />
						<div class="my-auto">
							search text: {{ searchText }}
						</div>
						<div class="my-auto">
							value: {{ value }}
						</div>
					</div>`
			}
		}}
	</Story>
</Canvas>

<Source
	language="html"
	/*html*/
	code={`<vv-dropdown
	v-model:modelValue="value"
	:options="options"
	label="Label 1"
	searchable
	label-no-result="No results"
	search-placeholder="Search"
	debounce-search="1000"
/>`}
/>
