import { Canvas, Meta, Story } from '@storybook/addon-docs'
import VvDropdown from '@/components/VvDropdown/VvDropdown.vue'
import { VvDropdownProps } from '@/components/VvDropdown'
import { propsToObject } from '@/utils/ObjectUtilities'
import { dropdownTest } from './Dropdown.test'

<Meta
	title="Components/Dropdown"
	component={VvDropdown}
	args={{
		...propsToObject(VvDropdownProps),
		options: [
			{
				label: 'Option 1',
				value: 1
			},
			{
				label: 'Option 2',
				value: 2
			},
			{
				label: 'Option 3',
				value: 3
			}
		]
	}}
/>

export const Template = (args) => ({
	components: { VvDropdown },
	setup() {
		return { args }
	},
	data() {
		return {
			value: ''
		}
	},
	template: `
		<vv-dropdown v-bind="args" v-model="value" class="max-w-224 relative" />
		<div class="mt-24">Selected: <span data-testId="selected">{{ value }}</span></div>
		`
})

# VvDropdown

Stories of `VvDropdown`.
Let's define a story for our playground `dropdown`:

<Canvas>
	<Story name="vv-dropdown" play={dropdownTest}>
		{Template.bind({})}
	</Story>
</Canvas>
