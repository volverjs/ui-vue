import { Canvas, Meta, Story, Source } from '@storybook/addon-docs'
import VvDropdown from '@/components/VvDropdown/VvDropdown.vue'
import { Template } from './Dropdown.stories.mdx'
import { dropdownTest } from './Dropdown.test.ts'

<Meta title="Components/Dropdown/Props/disabled" />

# VvDropdown disabled

Let's define a story for our `disabled` dropdown.

<Canvas>
	<Story
		name="disabled"
		play={async (ctx) => {
			dropdownTest(ctx, {
				className: 'vv-dropdown--disabled',
				isClickDisabled: true
			})
		}}
		args={{
			options: ['Option 4', 'Option 5', 'Option 6'],
			disabled: true
		}}>
		{Template.bind({})}
	</Story>
</Canvas>

<Source
	language="html"
	code={`<vv-dropdown v-model="value" :options="options" disabled />`}
/>
