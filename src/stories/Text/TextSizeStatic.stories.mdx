import { Canvas, Meta, Story } from '@storybook/addon-docs'
import VvText from '../../components/Text/VvText.vue'

<Meta title="Components/Text/size/static" />

# Static size

Stories of `vv-text` static `size`.

## 12

Let's define a story for our text component `VvText` with size `12`:

<Canvas>
	<Story name="12">
		{() => {
			return {
				components: { VvText },
				template:
					/*html*/
					`<vv-text size="12">Size 12</vv-text>`
			}
		}}
	</Story>
</Canvas>

## 14

Let's define a story for our text component `VvText` with size `14`:

<Canvas>
	<Story name="14">
		{() => {
			return {
				components: { VvText },
				template:
					/*html*/
					`<vv-text size="14">Size 14</vv-text>`
			}
		}}
	</Story>
</Canvas>

## 16

Let's define a story for our text component `VvText` with size `16`:

<Canvas>
	<Story name="16">
		{() => {
			return {
				components: { VvText },
				template:
					/*html*/
					`<vv-text size="16">Size 16</vv-text>`
			}
		}}
	</Story>
</Canvas>

## 18

Let's define a story for our text component `VvText` with size `18`:

<Canvas>
	<Story name="18">
		{() => {
			return {
				components: { VvText },
				template:
					/*html*/
					`<vv-text size="18">Size 18</vv-text>`
			}
		}}
	</Story>
</Canvas>

## 20

Let's define a story for our text component `VvText` with size `20`:

<Canvas>
	<Story name="20">
		{() => {
			return {
				components: { VvText },
				template:
					/*html*/
					`<vv-text size="20">Size 20</vv-text>`
			}
		}}
	</Story>
</Canvas>

## 24

Let's define a story for our text component `VvText` with size `24`:

<Canvas>
	<Story name="24">
		{() => {
			return {
				components: { VvText },
				template:
					/*html*/
					`<vv-text size="24">Size 24</vv-text>`
			}
		}}
	</Story>
</Canvas>

## 30

Let's define a story for our text component `VvText` with size `30`:

<Canvas>
	<Story name="30">
		{() => {
			return {
				components: { VvText },
				template:
					/*html*/
					`<vv-text size="30">Size 30</vv-text>`
			}
		}}
	</Story>
</Canvas>

## 36

Let's define a story for our text component `VvText` with size `36`:

<Canvas>
	<Story name="36">
		{() => {
			return {
				components: { VvText },
				template:
					/*html*/
					`<vv-text size="36">Size 36</vv-text>`
			}
		}}
	</Story>
</Canvas>

## 48

Let's define a story for our text component `VvText` with size `48`:

<Canvas>
	<Story name="48">
		{() => {
			return {
				components: { VvText },
				template:
					/*html*/
					`<vv-text size="48">Size 48</vv-text>`
			}
		}}
	</Story>
</Canvas>

## 60

Let's define a story for our text component `VvText` with size `60`:

<Canvas>
	<Story name="60">
		{() => {
			return {
				components: { VvText },
				template:
					/*html*/
					`<vv-text size="60">Size 60</vv-text>`
			}
		}}
	</Story>
</Canvas>

## 72

Let's define a story for our text component `VvText` with size `72`:

<Canvas>
	<Story name="72">
		{() => {
			return {
				components: { VvText },
				template:
					/*html*/
					`<vv-text size="72">Size 72</vv-text>`
			}
		}}
	</Story>
</Canvas>

## 96

Let's define a story for our text component `VvText` with size `96`:

<Canvas>
	<Story name="96">
		{() => {
			return {
				components: { VvText },
				template:
					/*html*/
					`<vv-text size="96">Size 96</vv-text>`
			}
		}}
	</Story>
</Canvas>

## 128

Let's define a story for our text component `VvText` with size `128`:

<Canvas>
	<Story name="128">
		{() => {
			return {
				components: { VvText },
				template:
					/*html*/
					`<vv-text size="128">Size 128</vv-text>`
			}
		}}
	</Story>
</Canvas>
