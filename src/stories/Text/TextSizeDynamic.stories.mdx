import { Canvas, Meta, Story } from '@storybook/addon-docs'
import VvText from '../../components/Text/VvText.vue'

<Meta title="Components/Text/size/dynamic" />

# Dynamic size

Stories of `vv-text` dynamic `size`.

## xs

Let's define a story for our text component `VvText` with size `xs`:

<Canvas>
	<Story name="xs">
		{() => {
			return {
				components: { VvText },
				template:
					/*html*/
					`<vv-text size="xs">Size xs</vv-text>`
			}
		}}
	</Story>
</Canvas>

## sm

Let's define a story for our text component `VvText` with size `sm`:

<Canvas>
	<Story name="sm">
		{() => {
			return {
				components: { VvText },
				template:
					/*html*/
					`<vv-text size="sm">Size sm</vv-text>`
			}
		}}
	</Story>
</Canvas>

## base

Let's define a story for our text component `VvText` with size `base`:

<Canvas>
	<Story name="base">
		{() => {
			return {
				components: { VvText },
				template:
					/*html*/
					`<vv-text size="base">Size base</vv-text>`
			}
		}}
	</Story>
</Canvas>

## lg

Let's define a story for our text component `VvText` with size `lg`:

<Canvas>
	<Story name="lg">
		{() => {
			return {
				components: { VvText },
				template:
					/*html*/
					`<vv-text size="lg">Size lg</vv-text>`
			}
		}}
	</Story>
</Canvas>

## xl

Let's define a story for our text component `VvText` with size `xl`:

<Canvas>
	<Story name="xl">
		{() => {
			return {
				components: { VvText },
				template:
					/*html*/
					`<vv-text size="xl">Size xl</vv-text>`
			}
		}}
	</Story>
</Canvas>

## 2xl

Let's define a story for our text component `VvText` with size `2xl`:

<Canvas>
	<Story name="2xl">
		{() => {
			return {
				components: { VvText },
				template:
					/*html*/
					`<vv-text size="2xl">Size 2xl</vv-text>`
			}
		}}
	</Story>
</Canvas>

## 3xl

Let's define a story for our text component `VvText` with size `3xl`:

<Canvas>
	<Story name="3xl">
		{() => {
			return {
				components: { VvText },
				template:
					/*html*/
					`<vv-text size="3xl">Size 3xl</vv-text>`
			}
		}}
	</Story>
</Canvas>
