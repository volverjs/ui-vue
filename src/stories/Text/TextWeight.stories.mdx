import { Canvas, Meta, Story, Source } from '@storybook/addon-docs'
import VvText from '../../components/Text/VvText.vue'

<Meta title="Components/Text/weight" />

# Weight

Stories of `vv-text` `weight`.

## thin

Let's define a story for our text component `VvText` with weight `thin`:

<Canvas>
	<Story name="thin">
		{() => {
			return {
				components: { VvText },
				template:
					/*html*/
					`<vv-text weight="thin">Size thin</vv-text>`
			}
		}}
	</Story>
</Canvas>

<Source language="jsx" code={'<vv-text weight="thin">Size thin</vv-text>'} />

## extralight

Let's define a story for our text component `VvText` with weight `extralight`:

<Canvas>
	<Story name="extralight">
		{() => {
			return {
				components: { VvText },
				template:
					/*html*/
					`<vv-text weight="extralight">Size extralight</vv-text>`
			}
		}}
	</Story>
</Canvas>

## light

Let's define a story for our text component `VvText` with weight `light`:

<Canvas>
	<Story name="light">
		{() => {
			return {
				components: { VvText },
				template:
					/*html*/
					`<vv-text weight="light">Size light</vv-text>`
			}
		}}
	</Story>
</Canvas>

## normal

Let's define a story for our text component `VvText` with weight `normal`:

<Canvas>
	<Story name="normal">
		{() => {
			return {
				components: { VvText },
				template:
					/*html*/
					`<vv-text weight="normal">Size normal</vv-text>`
			}
		}}
	</Story>
</Canvas>

## medium

Let's define a story for our text component `VvText` with weight `medium`:

<Canvas>
	<Story name="medium">
		{() => {
			return {
				components: { VvText },
				template:
					/*html*/
					`<vv-text weight="medium">Size medium</vv-text>`
			}
		}}
	</Story>
</Canvas>

## semibold

Let's define a story for our text component `VvText` with weight `semibold`:

<Canvas>
	<Story name="semibold">
		{() => {
			return {
				components: { VvText },
				template:
					/*html*/
					`<vv-text weight="semibold">Size semibold</vv-text>`
			}
		}}
	</Story>
</Canvas>

## bold

Let's define a story for our text component `VvText` with weight `bold`:

<Canvas>
	<Story name="bold">
		{() => {
			return {
				components: { VvText },
				template:
					/*html*/
					`<vv-text weight="bold">Size bold</vv-text>`
			}
		}}
	</Story>
</Canvas>

## extrabold

Let's define a story for our text component `VvText` with weight `extrabold`:

<Canvas>
	<Story name="extrabold">
		{() => {
			return {
				components: { VvText },
				template:
					/*html*/
					`<vv-text weight="extrabold">Size extrabold</vv-text>`
			}
		}}
	</Story>
</Canvas>

## black

Let's define a story for our text component `VvText` with weight `black`:

<Canvas>
	<Story name="black">
		{() => {
			return {
				components: { VvText },
				template:
					/*html*/
					`<vv-text weight="black">Size black</vv-text>`
			}
		}}
	</Story>
</Canvas>
