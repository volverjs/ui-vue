import { Canvas, Meta, Story, Source, ArgsTable } from '@storybook/addon-docs'

import VvCheckboxGroup from '@/components/VvCheckboxGroup/VvCheckboxGroup.vue'
import VvCheckbox from '@/components/VvCheckbox/VvCheckbox.vue'

import { Template } from './CheckboxGroup.stories.mdx'

<Meta title="Components/CheckboxGroup/Valid" component={VvCheckboxGroup} />

# Valid & Valid label

<Canvas>
	<Story
		name="Valid"
		args={{
			name: 'vv-group-options-valid-label',
			options: [
				{ value: 1, name: 'First' },
				{ value: 2, name: 'Second' },
				{ value: 3, name: 'Third' }
			],
			optionLabel: (o) => `${o.name} option`,
			valid: true,
			validLabel: 'Campo corretto'
		}}>
		{Template.bind({})}
	</Story>
</Canvas>
