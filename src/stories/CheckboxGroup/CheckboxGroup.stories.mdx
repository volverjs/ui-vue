import { Canvas, Meta, Story, Source, ArgsTable } from '@storybook/addon-docs'

import VvCheckboxGroup from '@/components/VvCheckboxGroup/VvCheckboxGroup.vue'
import VvCheckbox from '@/components/VvCheckbox/VvCheckbox.vue'

<Meta title="Components/CheckboxGroup" component={VvCheckboxGroup} />

export const Template = (args, { argTypes }) => ({
	props: Object.keys(argTypes),
	components: { VvCheckboxGroup, VvCheckbox },
	setup() {
		return { args }
	},
	data: () => ({ selected: args.modelValue || [] }),
	template: `
		<div class="flex flex-row justify-between">
            <vv-checkbox-group v-bind="args" v-model="selected">
                <template v-slot:hint>
                    <p>Lorem ipsum dolor sit amet</p>
                </template> 
            </vv-checkbox-group>
            <strong v-if="selected">Selected options: {{selected}} </strong> 
        </div>
        `
})

# VvCheckboxGroup

A group of checks.

<Source language="jsx" code={'<vv-checkbox-group>...</vv-checkbox-group>'} />

<Canvas>
	<Story
		name="vv-checkbox-group"
		args={{
			name: 'story1',
			label: 'Label',
			vertical: false,
			disabled: false,
			readonly: false,
			options: [],
			optionLabel: 'label',
			optionValue: 'value',
			hintLabel: 'Lorem ipsum dolor sit amet'
		}}>
		{(args) => {
			return {
				components: { VvCheckboxGroup, VvCheckbox },
				data: () => ({ selected: args.modelValue || [] }),
				setup() {
					return { args }
				},
				template: `
                    <div class="flex flex-row justify-between">
                        <vv-checkbox-group v-bind="args" v-model="selected">
                            <vv-checkbox id="o1" name="opts" value="1">Option 1</vv-checkbox>
                            <vv-checkbox id="o2" name="opts" value="2">Option 2</vv-checkbox>
                            <vv-checkbox id="o3" name="opts" value="3">Option 3</vv-checkbox>
                        </vv-checkbox-group>
                        <strong v-if="selected">Selected options: {{selected}} </strong> 
                    </div>
                    `
			}
		}}
	</Story>
</Canvas>

<ArgsTable of={VvCheckboxGroup} />
