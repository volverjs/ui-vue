import { Canvas, Meta, Story, Source, ArgsTable } from '@storybook/addon-docs'

import VvCheckboxGroup from '@/components/VvCheckboxGroup/VvCheckboxGroup.vue'
import VvCheckbox from '@/components/VvCheckbox/VvCheckbox.vue'

import { Template } from './CheckboxGroup.stories.mdx'

<Meta
	title="Components/CheckboxGroup/OptionValue"
	component={VvCheckboxGroup}
/>

# OptionValue

Change object prop for value

<Canvas>
	<Story
		name="OptionValue - string"
		args={{
			name: 'vv-group-options-value-string',
			options: [
				{ qta: 1, label: 'Option 1' },
				{ qta: 2, label: 'Option 2' },
				{ qta: 3, label: 'Option 3' }
			],
			optionValue: 'qta'
		}}>
		{Template.bind({})}
	</Story>
</Canvas>

# OptionValue Function

Use function to define option value

<Canvas>
	<Story
		name="OptionValue - function"
		args={{
			name: 'vv-group-options-value-function',
			options: [
				{ qta: 1, label: 'Option 1' },
				{ qta: 2, label: 'Option 2' },
				{ qta: 3, label: 'Option 3' }
			],
			optionValue: (o) => `${o.qta}_${o.label}`
		}}>
		{Template.bind({})}
	</Story>
</Canvas>
