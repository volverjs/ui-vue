import { Canvas, Meta, Story, Source, ArgsTable } from '@storybook/addon-docs'

import VvRadioGroup from '@/components/VvRadioGroup/VvRadioGroup.vue'
import VvRadio from '@/components/VvRadio/VvRadio.vue'

import { radioGroupTest } from './RadioProperty.test.ts'
import { Template } from './RadioGroup.stories.mdx'

<Meta title="Components/RadioGroup/Error" component={VvRadioGroup} />

# Error & Error message

<Canvas>
	<Story
		name="Error"
		play={async (ctx) => {
			radioGroupTest(ctx, {
				className: 'vv-input-radio-group--invalid'
			})
		}}
		args={{
			name: 'error',
			options: [
				{ value: 1, name: 'First' },
				{ value: 2, name: 'Second' },
				{ value: 3, name: 'Third' }
			],
			optionLabel: (o) => `${o.name} option`,
			error: true,
			errorLabel: ['First error', 'Second error'],
			role: 'radiogroup'
		}}>
		{Template.bind({})}
	</Story>
</Canvas>
