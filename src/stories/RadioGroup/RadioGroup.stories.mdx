import { Canvas, Meta, Story, Source, ArgsTable } from '@storybook/addon-docs'
import VvRadioGroup from '@/components/VvRadioGroup/VvRadioGroup.vue'
import VvRadio from '@/components/VvRadio/VvRadio.vue'
import { radioGroupTest } from './RadioGroup.test'

<Meta title="Components/RadioGroup" component={VvRadioGroup} />

export const Template = (args, { argTypes }) => ({
	props: Object.keys(argTypes),
	components: { VvRadioGroup, VvRadio },
	setup() {
		return { args }
	},
	data: () => ({ selected: args.modelValue || null }),
	template: `
		<vv-radio-group v-model="selected" v-bind="args" data-testId="radiogroup">
			<template v-slot:hint>
				<p>Lorem ipsum dolor sit amet</p>
			</template> 
		</vv-radio-group>
		<strong v-if="selected">Selected options: {{selected}} </strong> 
    `
})

# VvRadioGroup

A group of radios.

<Source language="jsx" code={'<vv-radio-group>...</vv-radio-group>'} />

<Canvas>
	<Story
		name="vv-radio-group"
		play={radioGroupTest}
		args={{
			name: 'radio',
			label: 'Test',
			vertical: false,
			disabled: false,
			readonly: false,
			options: [],
			optionLabel: 'label',
			optionValue: 'value',
			hintLabel: 'Lorem ipsum dolor sit amet'
		}}>
		{(args) => {
			return {
				components: { VvRadioGroup, VvRadio },
				data: () => ({ selected: args.modelValue || null }),
				setup() {
					return { args }
				},
				template: `
					<vv-radio-group v-model="selected" v-bind="args" data-testId="radiogroup">
						<vv-radio id="radio_opt1" name="opts" value="1">Option 1</vv-radio>
						<vv-radio id="radio_opt2" name="opts" value="2">Option 2</vv-radio>
						<vv-radio id="o3" name="opts" value="3">Option 3</vv-radio>
					</vv-radio-group>
					<strong v-if="selected">Selected options: {{selected}} </strong> 
    			`
			}
		}}
	</Story>
</Canvas>

<ArgsTable of={VvRadioGroup} />
