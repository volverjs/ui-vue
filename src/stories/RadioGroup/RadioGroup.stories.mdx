import { Canvas, Meta, Story, Source, ArgsTable } from '@storybook/addon-docs'

import VvRadioGroup from '../../components/VvRadioGroup/VvRadioGroup.vue'
import VvRadio from '../../components/VvRadio/VvRadio.vue'

<Meta title="Components/RadioButtonGroup" component={VvRadioGroup} />

# VvRadioGroup

A group of radios.

<Source language="jsx" code={'<vv-radio-group>...</vv-radio-group>'} />

<Canvas>
	<Story
		name="vv-radio-group"
		args={{ name: 'story1', vertical: false, disabled: false }}>
		{(args) => {
			return {
				components: { VvRadioGroup, VvRadio },
				data: () => ({ selected: null, d1: false }),
				setup() {
					return { args }
				},
				template:
					/*html*/
					`
                    <div class="flex flex-row justify-between">
                        <vv-radio-group v-bind="args" v-model:modelValue="selected">
                            <vv-radio id="o1" name="opts" value="1">Opt 1</vv-radio>
                            <vv-radio id="o2" name="opts" value="2">Opt 2</vv-radio>
                            <vv-radio id="o3" name="opts" value="3">Opt 3</vv-radio>
                        </vv-radio-group>
                        <strong v-if="selected">Bottone selez: {{selected}} </strong> 
                    </div>
                    `
			}
		}}
	</Story>
</Canvas>

<ArgsTable of="{VvRadioGroup}" />
