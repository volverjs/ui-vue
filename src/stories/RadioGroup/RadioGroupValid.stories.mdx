import { Canvas, Meta, Story, Source, ArgsTable } from '@storybook/addon-docs'

import VvRadioGroup from '../../components/VvRadioGroup/VvRadioGroup.vue'
import VvRadio from '../../components/VvRadio/VvRadio.vue'

import { validTest } from './RadioPropertyTest.js'
import { Template } from './RadioGroup.stories.mdx'

<Meta title="Components/RadioGroup/Valid" component={VvRadioGroup} />

# Valid & Valid label

<Canvas>
	<Story
		name="Valid"
		play={validTest}
		args={{
			name: 'vv-group-options-valid-label',
			options: [
				{ value: 1, name: 'Mela' },
				{ value: 2, name: 'Pera' },
				{ value: 3, name: 'Banana' }
			],
			optionLabel: (o) => `Frutto: ${o.name}`,
			valid: true,
			validLabel: 'Campo corretto',
			role: 'radiogroup'
		}}>
		{Template.bind({})}
	</Story>
</Canvas>
