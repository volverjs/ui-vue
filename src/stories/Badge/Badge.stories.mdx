import { Canvas, Meta, Story } from '@storybook/addon-docs'
import VvBadge from '@/components/VvBadge/VvBadge.vue'
import { defaultTest } from './Badge.test'
import { defaultArgs, argTypes } from './Badge.settings'

<Meta
	title="Components/Badge"
	component={VvBadge}
	args={defaultArgs}
	argTypes={argTypes}
/>

export const Template = (args, { argTypes }) => ({
	props: Object.keys(argTypes),
	components: { VvBadge },
	setup() {
		return { args }
	},
	template: /*html*/ `
		<vv-badge v-bind="args">
			<template #default v-if="args.default"><span v-html="args.default"></span></template>	
		</vv-badge>`,
})

<Canvas>
	<Story name="Default" args={{ value: '14' }} play={defaultTest}>
		{Template.bind()}
	</Story>
</Canvas>

<Canvas>
	<Story
		name="Dot"
		args={{ value: '' }}
		argTypes={{
			default: {
				control: {
					disable: true,
				},
			},
			value: {
				control: {
					disable: true,
				},
			},
		}}
		play={defaultTest}
	>
		{Template.bind()}
	</Story>
</Canvas>
