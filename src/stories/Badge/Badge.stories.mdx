import { Canvas, Meta, Story } from '@storybook/addon-docs'
import VvBadge from '../../components/VvBadge/VvBadge.vue'
import testBadgeVariant from './BadgeTest.js'
import testDangerBadge from './BadgeTest.js'

<Meta
	title="Components/Badge"
	component={VvBadge}
	argTypes={{
		value: {
			defaultValue: '14',
			control: { type: 'text' }
		},
		color: {
			control: {
				type: 'select',
				options: ['', 'black', 'danger', 'success', 'warning']
			}
		}
	}}
/>

export const Template = (args) => ({
	components: { VvBadge },
	setup() {
		return { args }
	},
	template:
		/*html*/
		`<vv-badge v-bind="args" />`
})

# VvBadge

Stories of `VvBadge`.
Let's define a story for our playground `badge`:

<Story name="vv-badge" args={{}}>
	{Template.bind({})}
</Story>

<Story name="black"
	play={testBadgeVariant}
>
	{() => {
			return {
				components: { VvBadge },
				template:
					/*html*/
					`<vv-badge color="black" role="status" value="12" />`
			}
	}}
	
</Story>

<Story name="danger"
	play={testBadgeVariant}
>
	{() => {
			return {
				components: { VvBadge },
				template:
					/*html*/
					`<vv-badge color="danger" role="status" value="16" />`
			}
	}}
</Story>

<Story name="success"
	play={testBadgeVariant}
>
	{() => {
			return {
				components: { VvBadge },
				template:
					/*html*/
					`<vv-badge color="success" role="status" value="10" />`
			}
	}}
</Story>

<Story name="warning"
	play={testBadgeVariant}
>
	{() => {
			return {
				components: { VvBadge },
				template:
					/*html*/
					`<vv-badge color="warning" role="status" value="8" />`
			}
	}}
</Story>
