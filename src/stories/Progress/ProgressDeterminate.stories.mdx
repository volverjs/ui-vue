import { Canvas, Meta, Story, Source } from '@storybook/addon-docs'
import VvProgress from '@/components/VvProgress/VvProgress.vue'
import { Template } from './Progress.stories.mdx'

<Meta title="Components/Progress/Props" component={VvProgress} />

# VvProgress determinate

Stories of `VvProgress`.
Let's define a story for our `determinate` progress with `max` and `value`:

<Story name="determinate" args={{ determinate: true, max: '100', value: '25' }}>
	{Template.bind({})}
</Story>

<Source
	language="html"
	code={`<vv-progress max="100" value="25" determinate />`}
/>
