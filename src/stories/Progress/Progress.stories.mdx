import { Canvas, Meta, Story } from '@storybook/addon-docs'
import VvProgress from '../../components/VvProgress/VvProgress.vue'
import { progressTest, indeterminateTest } from './ProgressTest.js'
import ObjectUtilities from '../../utils/ObjectUtilities'
import { VvProgressProps } from '../../components/VvProgress/VvProgress'

<Meta
	title="Components/Progress"
	component={VvProgress}
	args={{
		...ObjectUtilities.propsToObject(VvProgressProps)
	}}
	argTypes={{
		value: {
			defaultValue: 30
		},
		max: {
			defaultValue: 100
		},
		determinate: false
	}}
/>

export const Template = (args) => ({
	components: { VvProgress },
	setup() {
		return { args }
	},
	template: `<vv-progress v-bind="args" />`
})

# VvProgress

Stories of `VvProgress`.

Let's define a story for our playground `progress`:

<Story name="vv-progress" play={indeterminateTest}>
	{Template.bind()}
</Story>
