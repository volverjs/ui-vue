import { Canvas, Meta, Story, Source, ArgsTable } from '@storybook/addon-docs'
import { action } from '@storybook/addon-actions'

import VvInputText from '@/components/VvInputText/VvInputText.vue'
import { Template } from './InputText.stories.mdx'
import { testInputText } from './InputText.test.ts'

<Meta title="Components/InputText/Min" component={VvInputText} />

<Canvas>
	<Story
		name="Min - DateTime"
		play={testInputText}
		args={{
			name: 'input-datetime-min',
			label: 'Input Datetime Min',
			type: 'date',
			min: new Date().toISOString().split('T')[0],
			'data-testId': 'input-text'
		}}>
		{Template.bind({})}
	</Story>
</Canvas>

<Canvas>
	<Story
		name="Min - Number"
		play={testInputText}
		args={{
			name: 'input-number-min',
			label: 'Input Number Min',
			type: 'number',
			min: 5,
			'data-testId': 'input-text'
		}}>
		{Template.bind({})}
	</Story>
</Canvas>
