import { Canvas, Meta, Story, Source, ArgsTable } from '@storybook/addon-docs'
import { action } from '@storybook/addon-actions'

import VvInputText from '@/components/VvInputText/VvInputText.vue'
import { Template } from './InputText.stories.mdx'

<Meta title="Components/InputText/Type" component={VvInputText} />

<Canvas>
	<Story name="Text" args={{ name: 'input-text', type: 'text' }}>
		{Template.bind({})}
	</Story>
</Canvas>

<Canvas>
	<Story name="Password" args={{ name: 'input-password', type: 'password' }}>
		{Template.bind({})}
	</Story>
</Canvas>

<Canvas>
	<Story name="Date" args={{ name: 'input-date', type: 'date' }}>
		{Template.bind({})}
	</Story>
</Canvas>

<Canvas>
	<Story
		name="DateTime"
		args={{ name: 'input-datetime', type: 'datetime-local' }}>
		{Template.bind({})}
	</Story>
</Canvas>

<Canvas>
	<Story name="Number" args={{ name: 'input-number', type: 'number' }}>
		{Template.bind({})}
	</Story>
</Canvas>

<Canvas>
	<Story name="Time" args={{ name: 'input-time', type: 'time' }}>
		{Template.bind({})}
	</Story>
</Canvas>

<Canvas>
	<Story name="Email" args={{ name: 'input-email', type: 'email' }}>
		{Template.bind({})}
	</Story>
</Canvas>

<Canvas>
	<Story name="Tel" args={{ name: 'input-tel', type: 'tel' }}>
		{Template.bind({})}
	</Story>
</Canvas>

<Canvas>
	<Story name="Url" args={{ name: 'input-url', type: 'url' }}>
		{Template.bind({})}
	</Story>
</Canvas>

<Canvas>
	<Story name="Color" args={{ name: 'input-color', type: 'color' }}>
		{Template.bind({})}
	</Story>
</Canvas>

<Canvas>
	<Story name="Search" args={{ name: 'input-search', type: 'search' }}>
		{Template.bind({})}
	</Story>
</Canvas>

<Canvas>
	<Story name="File" args={{ name: 'input-file', type: 'file' }}>
		{Template.bind({})}
	</Story>
</Canvas>
