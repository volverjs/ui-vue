import { Canvas, Meta, Story, Source, ArgsTable } from '@storybook/addon-docs'
import { action } from '@storybook/addon-actions'

import VvInputText from '../../components/VvInputText/VvInputText.vue'
import { Template } from './InputText.stories.mdx'

<Meta title="Components/InputText/IconPosition" component={VvInputText} />

<Canvas>
	<Story
		name="Left"
		args={{
			name: 'input-text-icon',
			icon: 'add',
			iconPosition: 'left'
		}}>
		{Template.bind({})}
	</Story>
</Canvas>

<Canvas>
	<Story
		name="Right"
		args={{
			name: 'input-text-icon',
			icon: 'add',
			iconPosition: 'right'
		}}>
		{Template.bind({})}
	</Story>
</Canvas>
