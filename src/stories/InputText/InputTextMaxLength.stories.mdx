import { Canvas, Meta, Story, Source, ArgsTable } from '@storybook/addon-docs'
import { action } from '@storybook/addon-actions'

import VvInputText from '@/components/VvInputText/VvInputText.vue'
import { Template } from './InputText.stories.mdx'
import { testInputText } from './InputText.test.ts'
import { limitTest } from './InputText.test.ts'

<Meta title="Components/InputText/MaxLength" component={VvInputText} />

<Canvas>
	<Story
		name="MaxLength"
		play={testInputText}
		args={{
			name: 'input-text-min-length',
			label: 'Input Text Max Length',
			maxlength: 5,
			'data-testId': 'input-text'
		}}>
		{Template.bind({})}
	</Story>
</Canvas>

<Canvas>
	<Story
		name="MaxLength - limit"
		play={limitTest}
		args={{
			name: 'input-text-limit',
			label: 'Input Text Limit',
			maxlength: 5,
			limit: true,
			'data-testId': 'input-text'
		}}>
		{Template.bind({})}
	</Story>
</Canvas>

<Canvas>
	<Story
		name="MaxLength - countdown"
		play={limitTest}
		args={{
			name: 'input-text-limit-countdown',
			label: 'Input Text Countdown',
			maxlength: 5,
			limit: 'countdown',
			'data-testId': 'input-text'
		}}>
		{Template.bind({})}
	</Story>
</Canvas>
