import { Canvas, Meta, Story, Source, ArgsTable } from '@storybook/addon-docs'
import { action } from '@storybook/addon-actions'

import VvInputText from '@/components/VvInputText/VvInputText.vue'
import { Template } from './InputText.stories.mdx'
import { testInputText } from './InputText.test.ts'

<Meta title="Components/InputText/Loading" component={VvInputText} />

<Canvas>
	<Story
		name="Loading"
		play={async (ctx) => {
			testInputText(ctx, {
				className: 'vv-input-text--loading'
			})
		}}
		args={{
			name: 'input-text-loading',
			label: 'Input Text Loading',
			loading: true,
			loadingLabel: 'Loading label!',
			'data-testId': 'input-text'
		}}>
		{Template.bind({})}
	</Story>
</Canvas>
