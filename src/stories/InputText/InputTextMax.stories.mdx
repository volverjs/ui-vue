import { Canvas, Meta, Story, Source, ArgsTable } from '@storybook/addon-docs'
import { action } from '@storybook/addon-actions'

import VvInputText from '../../components/VvInputText/VvInputText.vue'
import { Template } from './InputText.stories.mdx'
import { testInputText } from './InputText.test.ts'

<Meta title="Components/InputText/Max" component={VvInputText} />

<Canvas>
	<Story
		name="Max - DateTime"
		play={testInputText}
		args={{
			name: 'input-datetime-max',
			type: 'date',
			max: new Date().toISOString().split('T')[0],
			'data-testId': 'input-text'
		}}>
		{Template.bind({})}
	</Story>
</Canvas>

<Canvas>
	<Story
		name="Max - Number"
		play={testInputText}
		args={{
			name: 'input-number-max',
			type: 'number',
			max: 15,
			'data-testId': 'input-text'
		}}>
		{Template.bind({})}
	</Story>
</Canvas>
