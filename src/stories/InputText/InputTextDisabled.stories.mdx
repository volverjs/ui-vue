import { Canvas, Meta, Story, Source, ArgsTable } from '@storybook/addon-docs'
import { action } from '@storybook/addon-actions'

import VvInputText from '../../components/VvInputText/VvInputText.vue'
import { Template } from './InputText.stories.mdx'
import { testInputText } from './InputText.test.ts'

<Meta title="Components/InputText/Disabled" component={VvInputText} />

<Canvas>
	<Story
		name="Disabled"
		play={(ctx) => {
			testInputText(ctx, {
				isClickDisabled: true
			})
		}}
		args={{
			name: 'input-text-disabled',
			disabled: true,
			modelValue: 'Prova',
			'data-testId': 'input-text'
		}}>
		{Template.bind({})}
	</Story>
</Canvas>
