import { Canvas, Meta, Story, Source, ArgsTable } from '@storybook/addon-docs'
import { action } from '@storybook/addon-actions'

import VvInputText from '../../components/VvInputText/VvInputText.vue'
import { Template } from './InputText.stories.mdx'

<Meta title="Components/InputText/Step" component={VvInputText} />

<Canvas>
	<Story
		name="Step"
		args={{
			name: 'input-number-step',
			type: 'number',
			step: 1
		}}>
		{Template.bind({})}
	</Story>
</Canvas>

<Canvas>
	<Story
		name="Step - decimal"
		args={{
			name: 'input-number-step-decimal',
			type: 'number',
			step: 0.5
		}}>
		{Template.bind({})}
	</Story>
</Canvas>
