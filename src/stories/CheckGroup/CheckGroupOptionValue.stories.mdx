import { Canvas, Meta, Story, Source, ArgsTable } from '@storybook/addon-docs'

import VvCheckGroup from '../../components/VvCheckGroup/VvCheckGroup.vue'
import VvCheck from '../../components/VvCheck/VvCheck.vue'

import { Template } from './CheckGroup.stories.mdx'

<Meta title="Components/CheckGroup/OptionValue" component={VvCheckGroup} />

# OptionValue

Change object prop for value

<Canvas>
	<Story
		name="OptionValue - string"
		args={{
			name: 'vv-group-options-value-string',
			options: [
				{ qta: 1, label: 'Mela' },
				{ qta: 2, label: 'Pera' },
				{ qta: 3, label: 'Banana' }
			],
			optionValue: 'qta'
		}}>
		{Template.bind({})}
	</Story>
</Canvas>

# OptionValue Function

Use function to define option value

<Canvas>
	<Story
		name="OptionValue - function"
		args={{
			name: 'vv-group-options-value-function',
			options: [
				{ qta: 1, label: 'Mela' },
				{ qta: 2, label: 'Pera' },
				{ qta: 3, label: 'Banana' }
			],
			optionValue: (o) => `${o.qta}_${o.label}`
		}}>
		{Template.bind({})}
	</Story>
</Canvas>
