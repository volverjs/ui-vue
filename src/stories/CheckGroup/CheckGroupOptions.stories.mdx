import { Canvas, Meta, Story, Source, ArgsTable } from '@storybook/addon-docs'

import VvCheckGroup from '../../components/VvCheckGroup/VvCheckGroup.vue'
import VvCheck from '../../components/VvCheck/VvCheck.vue'

import { Template } from './CheckGroup.stories.mdx'

<Meta title="Components/CheckGroup/Options" component={VvCheckGroup} />

# Options

Check group based on list of object list

<Canvas>
	<Story
		name="Options"
		args={{
			name: 'story-options',
			options: [
				{ value: 1, label: 'Mela' },
				{ value: 2, label: 'Pera' },
				{ value: 3, label: 'Banana' }
			]
		}}>
		{Template.bind({})}
	</Story>
</Canvas>

# Options as Array

<Canvas>
	<Story
		name="Options as Array"
		args={{
			name: 'story-options-strings',
			options: ['Primo', 'Secondo', 'Terzo']
		}}>
		{Template.bind({})}
	</Story>
</Canvas>
