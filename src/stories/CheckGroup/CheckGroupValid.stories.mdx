import { Canvas, Meta, Story, Source, ArgsTable } from '@storybook/addon-docs'

import VvCheckGroup from '../../components/VvCheckGroup/VvCheckGroup.vue'
import VvCheck from '../../components/VvCheck/VvCheck.vue'

import { Template } from './CheckGroup.stories.mdx'

<Meta title="Components/CheckGroup/Valid" component={VvCheckGroup} />

# Valid & Valid label

<Canvas>
	<Story
		name="Valid"
		args={{
			name: 'vv-group-options-valid-label',
			options: [
				{ value: 1, name: 'Mela' },
				{ value: 2, name: 'Pera' },
				{ value: 3, name: 'Banana' }
			],
			optionLabel: (o) => `Frutto: ${o.name}`,
			valid: true,
			validLabel: 'Campo corretto'
		}}>
		{Template.bind({})}
	</Story>
</Canvas>
