import { Canvas, Meta, Story, Source, ArgsTable } from '@storybook/addon-docs'

import VvCheckGroup from '../../components/VvCheckGroup/VvCheckGroup.vue'
import VvCheck from '../../components/VvCheck/VvCheck.vue'

import { Template } from './CheckGroup.stories.mdx'

<Meta title="Components/CheckGroup/OptionLabel" component={VvCheckGroup} />

# OptionLabel

Change object prop for label

<Canvas>
	<Story
		name="OptionsLabel - string"
		args={{
			name: 'vv-group-options-label-string',
			options: [
				{ value: 1, name: 'Mela' },
				{ value: 2, name: 'Pera' },
				{ value: 3, name: 'Banana' }
			],
			optionLabel: 'name'
		}}>
		{Template.bind({})}
	</Story>
</Canvas>

# OptionLabel Function

Use function to define option label

<Canvas>
	<Story
		name="OptionsLabel - function"
		args={{
			name: 'vv-group-options-label-function',
			options: [
				{ value: 1, name: 'Mela' },
				{ value: 2, name: 'Pera' },
				{ value: 3, name: 'Banana' }
			],
			optionLabel: (o) => `Frutto: ${o.name}`
		}}>
		{Template.bind({})}
	</Story>
</Canvas>
