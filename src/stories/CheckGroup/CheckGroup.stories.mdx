import { Canvas, Meta, Story, Source, ArgsTable } from '@storybook/addon-docs'

import VvCheckGroup from '@/components/VvCheckGroup/VvCheckGroup.vue'
import VvCheck from '@/components/VvCheck/VvCheck.vue'

<Meta title="Components/CheckGroup" component={VvCheckGroup} />

export const Template = (args, { argTypes }) => ({
	props: Object.keys(argTypes),
	components: { VvCheckGroup, VvCheck },
	setup() {
		return { args }
	},
	data: () => ({ selected: args.modelValue || [] }),
	template: `
		<div class="flex flex-row justify-between">
            <vv-check-group v-bind="args" v-model="selected">
                <template v-slot:hint>
                    <p>Lorem ipsum dolor sit amet</p>
                </template> 
            </vv-check-group>
            <strong v-if="selected">Selected options: {{selected}} </strong> 
        </div>
        `
})

# VvCheckGroup

A group of checks.

<Source language="jsx" code={'<vv-radio-group>...</vv-radio-group>'} />

<Canvas>
	<Story
		name="vv-check-group"
		args={{
			name: 'story1',
			label: 'Label',
			vertical: false,
			disabled: false,
			readonly: false,
			options: [],
			optionLabel: 'label',
			optionValue: 'value',
			hintLabel: 'Lorem ipsum dolor sit amet'
		}}>
		{(args) => {
			return {
				components: { VvCheckGroup, VvCheck },
				data: () => ({ selected: args.modelValue || [] }),
				setup() {
					return { args }
				},
				template: `
                    <div class="flex flex-row justify-between">
                        <vv-check-group v-bind="args" v-model="selected">
                            <vv-check id="o1" name="opts" value="1">Option 1</vv-check>
                            <vv-check id="o2" name="opts" value="2">Option 2</vv-check>
                            <vv-check id="o3" name="opts" value="3">Option 3</vv-check>
                        </vv-check-group>
                        <strong v-if="selected">Selected options: {{selected}} </strong> 
                    </div>
                    `
			}
		}}
	</Story>
</Canvas>

<ArgsTable of={VvCheckGroup} />
