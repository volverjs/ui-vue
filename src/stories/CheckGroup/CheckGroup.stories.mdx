import { Canvas, Meta, Story, Source, ArgsTable } from '@storybook/addon-docs'

import VvCheckGroup from '../../components/VvCheckGroup/VvCheckGroup.vue'
import VvCheck from '../../components/VvCheck/VvCheck.vue'

<Meta title="Components/CheckGroup" component={VvCheckGroup} />

# VvCheckGroup

A group of checks.

<Source language="jsx" code={'<vv-radio-group>...</vv-radio-group>'} />

<Canvas>
	<Story
		name="vv-check-group"
		args={{
			name: 'story1',
			label: 'Prova',
			vertical: false,
			disabled: false,
			readonly: false,
			options: [],
			optionLabel: 'label',
			optionValue: 'value',
			hintLabel: 'Ciccio'
		}}>
		{(args) => {
			return {
				components: { VvCheckGroup, VvCheck },
				data: () => ({ selected: args.modelValue || [] }),
				setup() {
					return { args }
				},
				template:
					/*html*/
					`
                    <div class="flex flex-row justify-between">
                        <vv-check-group v-bind="args" v-model:modelValue="selected">
                            <vv-check id="o1" name="opts" value="1">Opt 1</vv-check>
                            <vv-check id="o2" name="opts" value="2">Opt 2</vv-check>
                            <vv-check id="o3" name="opts" value="3">Opt 3</vv-check>
                        </vv-check-group>
                        <strong v-if="selected">Opzione selezionato: {{selected}} </strong> 
                    </div>
                    `
			}
		}}
	</Story>
</Canvas>

<ArgsTable of="{VvCheckGroup}" />

# VvCheckGroup DOM

<Canvas>
	<Story
		name="vv-check-group-dom"
		args={{ name: 'story1', vertical: false, disabled: false }}>
		{(args) => {
			return {
				components: { VvCheck },
				data: () => ({ selected: [], d1: false }),
				setup() {
					return { args }
				},
				template:
					/*html*/
					`
                    <div class="flex flex-row justify-between">
                        <div class="vv-input-check-group">
                            <vv-check v-model:modelValue="selected" id="o1" name="opts" value="1">Opt 1</vv-check>
                            <vv-check v-model:modelValue="selected" id="o2" name="opts" value="2">Opt 2</vv-check>
                            <vv-check v-model:modelValue="selected" id="o3" name="opts" value="3">Opt 3</vv-check>
                        </div>
                        <strong v-if="selected">Bottone selez: {{selected}} </strong> 
                    </div>
                    `
			}
		}}
	</Story>
</Canvas>
