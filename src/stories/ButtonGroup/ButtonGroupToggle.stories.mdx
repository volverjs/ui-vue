import { Canvas, Meta, Story } from '@storybook/addon-docs'
import VvButtonGroup from '@/components/VvButtonGroup/VvButtonGroup.vue'
import { Template } from './ButtonGroup.stories.mdx'
import { defaultArgs, argTypes } from './ButtonGroup.settings'
import { defaultTest } from './ButtonGroup.test'

<Meta
	title="Components/ButtonGroup/Toggle"
	component={VvButtonGroup}
	args={defaultArgs}
	argTypes={argTypes}
/>

<Canvas>
	<Story name="Default" play={defaultTest}>
		{Template.bind()}
	</Story>
</Canvas>

<Canvas>
	<Story
		name="Multiple"
		args={{ toggle: true, multiple: true }}
		argTypes={{
			toggle: {
				control: {
					disable: true
				}
			},
			multiple: {
				control: {
					disable: true
				}
			}
		}}
		play={defaultTest}>
		{Template.bind()}
	</Story>
</Canvas>

<Canvas>
	<Story
		name="Unselectable"
		args={{ toggle: true, unselectable: false }}
		argTypes={{
			toggle: {
				control: {
					disable: true
				}
			},
			unselectable: {
				control: {
					disable: true
				}
			}
		}}
		play={defaultTest}>
		{Template.bind()}
	</Story>
</Canvas>
