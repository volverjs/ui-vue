import { Canvas, Meta, Story, Source } from '@storybook/addon-docs'

import VvButton from '../components/VvButton/VvButton.vue'
import VvButtonGroup from '../components/VvButtonGroup/VvButtonGroup.vue'

<Meta title="Components/VvButtonGroup" component={VvButtonGroup} />

export const Template = (args, { argTypes }) => ({
	props: Object.keys(argTypes),
	components: { VvButton, VvButtonGroup },
	setup() {
		return { args }
	},
	template:
		/*html*/
		`
        <vv-button-group v-bind="args">
            <vv-button name="1a">Button 1</vv-button>
            <vv-button name="2b">Button 2</vv-button>
            <vv-button name="3c">Button 3</vv-button>
        </vv-button-group>
        `
})

# VvButtonGroup

Wrapper component for <strong>.vv-button</strong> elements or VvButton components.

<Source language="jsx" code={'<vv-button></vv-button>'} />

<Canvas>
	<Story name="vv-button-group" args={{ vertical: false, compact: true }}>
		{Template.bind({})}
	</Story>
</Canvas>
