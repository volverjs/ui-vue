import { Meta, Story } from '@storybook/addon-docs'
import VvRadio from '@/components/VvRadio/VvRadio.vue'
import { defaultTest } from './Radio.test'
import { defaultArgs, argTypes } from './Radio.settings'
import { Template } from './Radio.stories.mdx'

<Meta
	title="Components/Radio/Slots"
	component={VvRadio}
	args={defaultArgs}
	argTypes={argTypes}
/>

<Canvas>
	<Story
		name="Default"
		play={defaultTest}
		args={{ default: 'Default <em class="italic">slot!</em>' }}
		argTypes={{
			default: {
				control: {
					disable: true,
				},
			},
		}}
	>
		{Template.bind()}
	</Story>
</Canvas>

<Canvas>
	<Story
		name="Hint"
		play={defaultTest}
		args={{ hint: 'Hint <em class="italic">slot!</em>' }}
		argTypes={{
			hint: {
				control: {
					disable: true,
				},
			},
		}}
	>
		{Template.bind()}
	</Story>
</Canvas>
