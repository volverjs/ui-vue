import { Canvas, Meta, Story, Source } from '@storybook/addon-docs'
import { action } from '@storybook/addon-actions'

import VvRadio from '../../components/VvRadio/VvRadio.vue'
import { radioTest, hintLabelTest } from './Radio.test.ts'
import { Template } from './Radio.stories.mdx'

<Meta
	title="Components/RadioButton/Error"
	args={{
		modelValue: null
	}}
	component={VvRadio}
/>

# Error

<Canvas>
	<Story
		name="Invalid"
		play={radioTest}
		args={{
			id: 6,
			label: 'Voglio acquistare questa enciclopedia!',
			name: 'ck6',
			modelValue: false,
			error: true
		}}>
		{Template.bind({})}
	</Story>
</Canvas>

# Error with message

<Canvas>
	<Story
		name="Error with message"
		play={hintLabelTest}
		args={{
			id: 7,
			label: 'Voglio acquistare questa enciclopedia!',
			name: 'ck7',
			modelValue: false,
			error: true,
			errorLabel: 'Sbagliato!'
		}}>
		{Template.bind({})}
	</Story>
</Canvas>

# Error with messages

<Canvas>
	<Story
		name="Error with messages"
		play={hintLabelTest}
		args={{
			id: 8,
			label: 'Voglio acquistare questa enciclopedia!',
			name: 'ck8',
			modelValue: false,
			error: true,
			errorLabel: ['Sbagliato!', 'Mona!']
		}}>
		{Template.bind({})}
	</Story>
</Canvas>
