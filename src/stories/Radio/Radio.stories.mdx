import { Canvas, Meta, Story, Source, ArgsTable } from '@storybook/addon-docs'
import { action } from '@storybook/addon-actions'

import VvRadio from '@/components/VvRadio/VvRadio.vue'

<Meta
	title="Components/RadioButton"
	argTypes={{
		onClick: { action: 'click', table: { disable: true } },
		onChange: { action: 'change', table: { disable: true } },
		onFocus: { action: 'focus', table: { disable: true } },
		onBlur: { action: 'blur', table: { disable: true } }
	}}
	component={VvRadio}
/>

export const Template = (args, { argTypes }) => ({
	props: Object.keys(argTypes),
	components: { VvRadio },
	setup() {
		return { args }
	},
	data: () => ({ ckValue: args.modelValue }),
	template: `
        <div class="flex flex-row justify-between">
			<vv-radio v-bind="args" v-model="ckValue"/>
			<strong >Selected value: {{ckValue}} </strong> 
		</div>
        `
})

# VvRadio

A radio allows the user to select one option.

<Source language="jsx" code={'<vv-radio/>'} />

<Canvas>
	<Story
		name="vv-radio"
		args={{
			id: 1,
			label: 'opzione 1',
			name: 'o1',
			disabled: false,
			modelValue: null,
			value: { a: 1 }
		}}
		argTypes={{
			id: {
				description: 'Input id',
				table: { category: 'Attributes' }
			},
			name: {
				description: 'Input name',
				table: { category: 'Attributes' }
			},
			value: {
				description: 'Input value',
				table: { category: 'Attributes' }
			}
		}}>
		{Template.bind({})}
	</Story>
</Canvas>

<ArgsTable story="vv-radio" />
