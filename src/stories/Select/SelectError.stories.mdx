import { Canvas, Meta, Story, Source } from '@storybook/addon-docs'
import VvSelect from '../../components/VvSelect/VvSelect.vue'
import { Template } from './Select.stories.mdx'

<Meta title="Components/Select/Props/error" />

# VvSelect error/error-label

Let's define a story for our `error` and `error-label` select.

<Canvas>
	<Story
		name="error"
		args={{
			label: 'Lorem Ipsum',
			placeholder: 'Placeholder',
			options: ['Option 1', 'Option 2', 'Option 3'],
			error: true,
			errorLabel: 'An error occurred!'
		}}>
		{Template.bind({})}
	</Story>
</Canvas>

<Source
	language="html"
	/*html*/
	code={`
<vv-select
	v-model:modelValue="value"
	:options="options"
	placeholder="Placeholder"
	label="Label 1"
	error
	error-label="An error occurred!"
/>`}
/>
