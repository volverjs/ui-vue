import { Canvas, Meta, Story } from '@storybook/addon-docs'
import VvSelect from '../../components/VvSelect/VvSelect.vue'
import { VvSelectProps } from '../../components/VvSelect/VvSelect'
import ObjectUtilities from '../../utils/ObjectUtilities'

<Meta
	title="Components/Select"
	component={VvSelect}
	args={{
		...ObjectUtilities.propsToObjectInstance(VvSelectProps),
		label: 'Lorem Ipsum',
		placeholder: 'Placeholder',
		options: [
			{
				label: 'Option 1',
				value: 1
			},
			{
				label: 'Option 2',
				value: 2
			},
			{
				label: 'Option 3',
				value: 3
			}
		]
	}}
/>

export const Template = (args) => ({
	components: { VvSelect },
	setup() {
		return { args }
	},
	data() {
		return {
			selectValue: ''
		}
	},
	template:
		/*html*/
		`<div class="w-1/2">
            <vv-select v-bind="args" v-model:modelValue="selectValue" />
			value: {{ selectValue }}
        </div>`
})

# VvSelect

Stories of `VvSelect`.
Let's define a story for our playground `select`:

<Canvas>
	<Story name="vv-select">{Template.bind({})}</Story>
</Canvas>
