import { Canvas, Meta, Story, Source } from '@storybook/addon-docs'
import VvSelect from '../../components/VvSelect/VvSelect.vue'
import { Template } from './Select.stories.mdx'
import { selectTest } from './Select.test.ts'

<Meta title="Components/Select/Props/disabled" />

# VvSelect disabled

Let's define a story for our `disabled` select.

<Canvas>
	<Story
		name="disabled"
		play={(ctx) => {
			selectTest(ctx, {
				className: 'vv-select--disabled',
				isClickDisabled: true
			})
		}}
		args={{
			id: 1,
			label: 'Label 1',
			placeholder: 'Placeholder',
			disabled: true,
			value: '',
			options: ['Option 1', 'Option 2', 'Option 3']
		}}>
		{Template.bind({})}
	</Story>
</Canvas>

<Source
	language="html"
	code={`<vv-select v-model:modelValue="value" :options="options" placeholder="Placeholder" label="Label 1" disabled />`}
/>
