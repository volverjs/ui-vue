import { Canvas, Meta, Story, Source } from '@storybook/addon-docs'
import VvSelect from '../../components/VvSelect/VvSelect.vue'

<Meta title="Components/Select/Props/value-key_label-key" />

# VvSelect valueKey and labelKey

Let's define a story for our `valueKey` and `labelKey` select.

Options object array with custom `valueKey` ("val") and custom `labelKey` ("title"):

<Source
	language="json"
	/*html*/
	code={`[
	{
		"title": "Option 1",
		"val": 1
	},
	{
		"title": "Option 2",
		"val": 2
	},
	{
		"title": "Option 3",
		"val": 3
	}
]`}
/>

<Canvas>
	<Story name="value-key_label-key">
		{() => {
			return {
				components: { VvSelect },
				data() {
					return {
						value: '',
						options: [
							{
								title: 'Option 1',
								val: 1
							},
							{
								title: 'Option 2',
								val: 2
							},
							{
								title: 'Option 3',
								val: 3
							}
						]
					}
				},
				template:
					/*html*/
					`<div class="w-1/2">
						<vv-select v-model:modelValue="value" :options="options" value-key="val" label-key="title" />
						value: {{ value }}
					</div>`
			}
		}}
	</Story>
</Canvas>

<Source
	language="jsx"
	/*html*/
	code={`<vv-select v-model:modelValue="value" :options="options" value-key="val" label-key="title" />`}
/>
