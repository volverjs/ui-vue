import { Canvas, Meta, Story, Source } from '@storybook/addon-docs'
import VvSelect from '../../components/VvSelect/VvSelect.vue'
import { Template } from './Select.stories.mdx'
import { selectTest } from './Select.test.ts'

<Meta title="Components/Select/Props/useObject" />

# VvSelect useObject

Let's define a story for our `useObject` select.

Options object array and default `valueKey` ("value") and `labelKey` ("label"):

<Source
	language="json"
	code={`[
	{
		"label": "Option 1",
		"value": 1
	},
	{
		"label": "Option 2",
		"value": 2
	},
	{
		"label": "Option 3",
		"value": 3
	}
]`}
/>

<Canvas>
	<Story
		name="useObject"
		play={selectTest}
		args={{
			id: 1,
			label: 'Label 1',
			value: '',
			options: [
				{
					label: 'Option 1',
					value: 1
				},
				{
					label: 'Option 2',
					value: 2
				},
				{
					label: 'Option 3',
					value: 3
				}
			],
			useObject: true
		}}>
		{Template.bind({})}
	</Story>
</Canvas>

<Source
	language="html"
	code={`<vv-select v-model:modelValue="value" :options="options" label="Label 1" use-object />`}
/>
