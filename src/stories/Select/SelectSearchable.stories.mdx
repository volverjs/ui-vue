import { Canvas, Meta, Story, Source } from '@storybook/addon-docs'
import VvSelect from '../../components/VvSelect/VvSelect.vue'
import { selectTest, searchableTest } from './Select.test.ts'

<Meta title="Components/Select/Props/searchable" />

# VvSelect searchable

Let's define a story for our select with props:

`searchable` `label-no-result` `search-placeholder` and `debounce-search`

<Source
	language="json"
	code={`[
	{
		"label": "Option 1",
		"value": 1
	},
	{
		"label": "Option 2",
		"value": 2
	},
	{
		"label": "Option 3",
		"value": 3
	}
]`}
/>

<Canvas>
	<Story name="searchable" play={searchableTest}>
		{() => {
			return {
				components: { VvSelect },
				data() {
					return {
						value: '',
						searchText: '',
						options: [
							{
								label: 'Option 1',
								value: 1
							},
							{
								label: 'Option 2',
								value: 2
							},
							{
								label: 'Option 3',
								value: 3
							}
						]
					}
				},
				template: `<div class="w-1/2 flex gap-32">
						<vv-select id="select" v-model:modelValue="value" @change:search="searchText = $event" :options="options" label="Label 1" searchable label-no-result="No results" search-placeholder="Search" debounce-search="1000" />
						
					</div>
					<div id="searchText" class="my-auto">
							Search text: {{ searchText }}
						</div>
						<div class="my-auto">
							value: {{ value }}
						</div>`
			}
		}}
	</Story>
</Canvas>

<Source
	language="html"
	code={`<vv-select
	v-model:modelValue="value"
	:options="options"
	label="Label 1"
	searchable
	label-no-result="No results"
	search-placeholder="Search"
	debounce-search="1000"
/>`}
/>
