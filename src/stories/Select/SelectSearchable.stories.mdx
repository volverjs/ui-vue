import { Canvas, Meta, Story, Source } from '@storybook/addon-docs'
import VvSelect from '../../components/VvSelect/VvSelect.vue'

<Meta title="Components/Select/Props/searchable" />

# VvSelect searchable

Let's define a story for our select with props:

`searchable` `label-no-result` `search-placeholder` and `debounce-search`

<Source
	language="json"
	code={`[
	{
		"label": "Option 1",
		"value": 1
	},
	{
		"label": "Option 2",
		"value": 2
	},
	{
		"label": "Option 3",
		"value": 3
	}
]`}
/>

<Canvas>
	<Story name="searchable">
		{() => {
			return {
				components: { VvSelect },
				data() {
					return {
						value: '',
						searchText: '',
						options: [
							{
								label: 'Option 1',
								value: 1
							},
							{
								label: 'Option 2',
								value: 2
							},
							{
								label: 'Option 3',
								value: 3
							}
						]
					}
				},
				template: `<div class="w-1/2 flex gap-32">
						<vv-select v-model:modelValue="value" @change:search="searchText = $event" :options="options" label="Label 1" searchable label-no-result="No results" search-placeholder="Search" debounce-search="1000" />
						<div class="my-auto">
							search text: {{ searchText }}
						</div>
						<div class="my-auto">
							value: {{ value }}
						</div>
					</div>`
			}
		}}
	</Story>
</Canvas>

<Source
	language="html"
	code={`<vv-select
	v-model:modelValue="value"
	:options="options"
	label="Label 1"
	searchable
	label-no-result="No results"
	search-placeholder="Search"
	debounce-search="1000"
/>`}
/>
