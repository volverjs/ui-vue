import { Canvas, Meta, Story, Source } from '@storybook/addon-docs'
import VvSelect from '@/components/VvSelect/VvSelect.vue'

<Meta title="Components/Select/Props/maxValues" />

# VvSelect maxValues

Let's define a story for our multple `max-values` select.

<Canvas>
	<Story name="maxValues">
		{() => {
			return {
				components: { VvSelect },
				data() {
					return {
						value: '',
						options: ['Option 4', 'Option 5', 'Option 6']
					}
				},
				template: `<div class="w-1/2">
						<vv-select v-model="value" :options="options" label="Label 1" multiple max-values="2" />
						value: {{ value }}
					</div>`
			}
		}}
	</Story>
</Canvas>

<Source
	language="html"
	code={`<vv-select v-model="value" :options="options" label="Label 1" multiple max-values="2" />`}
/>
