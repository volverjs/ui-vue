import { Canvas, Meta, Story, Source } from '@storybook/addon-docs'
import VvSelect from '../../components/VvSelect/VvSelect.vue'
import { Template } from './Select.stories.mdx'
import { selectTest } from './Select.test.ts'

<Meta title="Components/Select/Props/maxValues" />

# VvSelect maxValues

Let's define a story for our multple `max-values` select.

<Canvas>
	<Story
		name="maxValues"
		play={selectTest}
		args={{
			id: 1,
			label: 'Label 1',
			multiple: true,
			maxValues: 2,
			value: '',
			options: ['Option 1', 'Option 2', 'Option 3']
		}}>
		{Template.bind({})}
	</Story>
</Canvas>

<Source
	language="html"
	code={`<vv-select v-model:modelValue="value" :options="options" label="Label 1" multiple max-values="2" />`}
/>
