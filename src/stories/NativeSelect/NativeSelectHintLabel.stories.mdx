import { Canvas, Meta, Story, Source } from '@storybook/addon-docs'
import VvNativeSelect from '@/components/VvNativeSelect/VvNativeSelect.vue'
import { Template } from './NativeSelect.stories.mdx'
import { hintLabelTest } from './NativeSelect.test'

<Meta title="Components/NativeSelect/Props/hint-label" />

# VvNativeSelect hint-label

Let's define a story for our `hint-label` select.

<Canvas>
	<Story
		name="hint-label"
		play={hintLabelTest}
		args={{
			name: 'select',
			label: 'Lorem Ipsum',
			placeholder: 'Placeholder',
			options: ['Option 1', 'Option 2', 'Option 3'],
			hintLabel: 'Information field',
			'data-testId': 'select'
		}}>
		{Template.bind({})}
	</Story>
</Canvas>

<Source
	language="html"
	code={`
<vv-select
	v-model="value"
	:options="options"
	placeholder="Placeholder"
	label="Label 1"
	hint-label="Information field"
/>`}
/>
