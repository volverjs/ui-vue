import { Canvas, Meta, Story } from '@storybook/addon-docs'
import VvNativeSelect from '@/components/VvNativeSelect/VvNativeSelect.vue'
import { VvNativeSelectProps } from '@/components/VvNativeSelect'
import { propsToObject } from '@/utils/ObjectUtilities'
import { nativeSelectTest } from './NativeSelect.test'

<Meta
	title="Components/NativeSelect"
	component={VvNativeSelect}
	args={{
		...propsToObject(VvNativeSelectProps),
		name: 'select',
		label: 'Lorem Ipsum',
		placeholder: 'Placeholder',
		options: [
			{
				label: 'Option 1',
				value: 1
			},
			{
				label: 'Option 2',
				value: 2
			},
			{
				label: 'Option 3',
				value: 3
			}
		],
		'data-testId': 'select'
	}}
/>

export const Template = (args) => ({
	components: { VvNativeSelect },
	setup() {
		return { args }
	},
	data() {
		return {
			selectValue: undefined
		}
	},
	template: `
		<vv-native-select v-bind="args" v-model="selectValue" />
		<div>Value: {{ selectValue }}</div>`
})

# VvNativeSelect

Stories of `VvNativeSelect`.
Let's define a story for our playground `select`:

<Canvas>
	<Story name="vv-native-select" play={nativeSelectTest}>
		{Template.bind({})}
	</Story>
</Canvas>
