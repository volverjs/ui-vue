import { Canvas, Meta, Story, Source } from '@storybook/addon-docs'
import VvNativeSelect from '@/components/VvNativeSelect/VvNativeSelect.vue'
import { Template } from './NativeSelect.stories.mdx'
import { errorTest } from './NativeSelect.test'

<Meta title="Components/NativeSelect/Props/error" />

# VvNativeSelect error/error-label

Let's define a story for our `error` and `error-label` select.

<Canvas>
	<Story
		name="error"
		play={errorTest}
		args={{
			name: 'select',
			label: 'Lorem Ipsum',
			placeholder: 'Placeholder',
			options: ['Option 1', 'Option 2', 'Option 3'],
			error: true,
			errorLabel: 'An error occurred!',
			'data-testId': 'select'
		}}>
		{Template.bind({})}
	</Story>
</Canvas>

<Source
	language="html"
	code={`
<vv-select
	v-model="value"
	:options="options"
	placeholder="Placeholder"
	label="Label 1"
	error
	error-label="An error occurred!"
/>`}
/>
