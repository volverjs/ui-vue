import { Canvas, Meta, Story, Source } from '@storybook/addon-docs'
import VvNativeSelect from '../../components/VvNativeSelect/VvNativeSelect.vue'
import { disabledTest } from './NativeSelectTest.js'

<Meta title="Components/NativeSelect/Props/disabled" />

# VvNativeSelect disabled

Let's define a story for our `disabled` select.

<Canvas>
	<Story name="disabled" play={disabledTest}>
		{() => {
			return {
				components: { VvNativeSelect },
				data() {
					return {
						value: '',
						options: ['Option 1', 'Option 2', 'Option 3']
					}
				},
				template:
					/*html*/
					`<div class="w-1/2">
						<vv-native-select data-testId='select' v-model:modelValue="value" :options="options" placeholder="Placeholder" label="Label 1" disabled />
					</div>`
			}
		}}
	</Story>
</Canvas>

<Source
	language="html"
	code={`<vv-native-select v-model:modelValue="value" :options="options" placeholder="Placeholder" label="Label 1" disabled />`}
/>
