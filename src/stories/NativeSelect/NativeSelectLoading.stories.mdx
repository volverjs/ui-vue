import { Canvas, Meta, Story, Source } from '@storybook/addon-docs'
import VvNativeSelect from '@/components/VvNativeSelect/VvNativeSelect.vue'
import { Template } from './NativeSelect.stories.mdx'
import { loadingTest } from './NativeSelectTest.js'

<Meta title="Components/NativeSelect/Props/loading" />

# VvNativeSelect loading

Let's define a story for our `loading` select.

<Canvas>
	<Story
		name="loading"
		play={loadingTest}
		args={{
			label: 'Lorem Ipsum',
			placeholder: 'Placeholder',
			options: ['Option 1', 'Option 2', 'Option 3'],
			loading: true,
			'data-testid': 'native-select'
		}}>
		{Template.bind({})}
	</Story>
</Canvas>

<Source
	language="html"
	code={`<vv-native-select v-model="value" :options="options" placeholder="Placeholder" label="Label 1" loading />`}
/>
