import { Canvas, Meta, Story, Source } from '@storybook/addon-docs'
import VvNativeSelect from '../../components/VvNativeSelect/VvNativeSelect.vue'
import { Template } from './NativeSelect.stories.mdx'
import { customKeysTest } from './NativeSelectTest.js'

<Meta title="Components/NativeSelect/Props/value-key_label-key" />

# VvNativeSelect valueKey and labelKey

Let's define a story for our `valueKey` and `labelKey` select.

Options object array with custom `valueKey` ("val") and custom `labelKey` ("title"):

<Source
	language="json"
	code={`[
	{
		"title": "Option 1",
		"val": 1
	},
	{
		"title": "Option 2",
		"val": 2
	},
	{
		"title": "Option 3",
		"val": 3
	}
]`}
/>

<Canvas>
	<Story
		name="value-key_label-key"
		play={customKeysTest}
		args={{
			label: 'Lorem Ipsum',
			placeholder: 'Placeholder',
			value: '',
			options: [
				{
					title: 'Option 1',
					val: 1
				},
				{
					title: 'Option 2',
					val: 2
				},
				{
					title: 'Option 3',
					val: 3
				}
			],
			'value-key': 'val',
			'label-key': 'title',
			'data-testid': 'native-select'
		}}>
		{Template.bind({})}
	</Story>
</Canvas>

<Source
	language="jsx"
	code={`<vv-native-select v-model:modelValue="value" :options="options" value-key="val" label-key="title" />`}
/>
