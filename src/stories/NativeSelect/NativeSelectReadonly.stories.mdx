import { Canvas, Meta, Story, Source } from '@storybook/addon-docs'
import VvNativeSelect from '@/components/VvNativeSelect/VvNativeSelect.vue'
import { Template } from './NativeSelect.stories.mdx'
import { readonlyTest } from './NativeSelect.test'

<Meta title="Components/NativeSelect/Props/readonly" />

# VvNativeSelect readonly

Let's define a story for our `readonly` select.

<Canvas>
	<Story name="readonly" play={readonlyTest}>
		{() => {
			return {
				components: { VvNativeSelect },
				data() {
					return {
						value: 'Option 1',
						options: ['Option 1', 'Option 2', 'Option 3']
					}
				},
				template: `
					<vv-native-select data-testid="native-select" placeholder="Select option" name="select" v-model="value" :options="options" label="Readonly" readonly />
					<div>Value : {{ value }}</div>
				`
			}
		}}
	</Story>
</Canvas>

<Source
	language="html"
	code={`
<vv-select
	v-model="value"
	:options="options"
	placeholder="Placeholder"
	label="Label 1"
	readonly
/>`}
/>
