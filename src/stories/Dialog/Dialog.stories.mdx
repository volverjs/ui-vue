import { Canvas, Meta, Story, Source } from '@storybook/addon-docs'
import { action } from '@storybook/addon-actions'

import VvDialog from '../../components/VvDialog/VvDialog.vue'

<Meta title="Components/Dialog" component={VvDialog} />

export const Template = (args, { argTypes }) => ({
	props: Object.keys(argTypes),
	components: { VvDialog },
	setup() {
		return { args }
	},
	data: () => ({ open: false }),
	template:
		/*html*/
		`
		<div class="flex flex-column">
			<button class="vv-button" @click="open=true">Open</button>
            <vv-dialog v-bind="args" v-model:open="open">
                Contenuto di prova
            </vv-dialog>
		</div>
        `
})

<Canvas>
	<Story name="Dialog" args={{ id: 'dialog' }}>
		{Template.bind({})}
	</Story>
</Canvas>
