import { Canvas, Meta, Story, Source } from '@storybook/addon-docs'
import { action } from '@storybook/addon-actions'

import VvDialog from '@/components/VvDialog/VvDialog.vue'

import { Template } from './Dialog.stories.mdx'

<Meta title="Components/Dialog/AutoClose" component={VvDialog} />

# Close on click outside

<Canvas>
	<Story
		name="AutoClose"
		args={{ id: 'dialog-autoclose', size: '', autoClose: true }}>
		{Template.bind({})}
	</Story>
</Canvas>
