import { Canvas, Meta, Story, Source } from '@storybook/addon-docs'
import { action } from '@storybook/addon-actions'

import VvCheckbox from '@/components/VvCheckbox/VvCheckbox.vue'

import { Template } from './Checkbox.stories.mdx'

<Meta title="Components/Checkbox/Error" component={VvCheckbox} />

# Error

<Canvas>
	<Story
		name="Error with message"
		args={{
			id: 7,
			label: 'Accept terms and conditions',
			name: 'ck7',
			value: 'conditions',
			error: true,
			errorLabel: 'Error label'
		}}>
		{Template.bind({})}
	</Story>
</Canvas>

# Error with messages

<Canvas>
	<Story
		name="Error with messages"
		args={{
			id: 8,
			label: 'Accept terms and conditions',
			name: 'ck8',
			value: 'conditions',
			error: true,
			errorLabel: ['First error', 'Second error']
		}}>
		{Template.bind({})}
	</Story>
</Canvas>
