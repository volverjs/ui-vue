import { Canvas, Meta, Story, Source } from '@storybook/addon-docs'
import { action } from '@storybook/addon-actions'

import VvCheckbox from '@/components/VvCheckbox/VvCheckbox.vue'

<Meta title="Components/Checkbox" component={VvCheckbox} />

export const Template = (args, { argTypes }) => ({
	props: Object.keys(argTypes),
	components: { VvCheckbox },
	setup() {
		return { args }
	},
	data: () => ({ ckValue: null }),
	template: `
		<div :data-testData="ckValue" data-testId="checkbox">
			<vv-checkbox v-bind="args" v-model="ckValue"/>
			<div>Selected value: {{ckValue}}</div> 
		</div>
        `
})

<Source language="jsx" code={'<vv-checkbox/>'} />

<Canvas>
	<Story
		name="vv-checkbox"
		args={{
			id: 1,
			label: 'Option 1',
			name: 'ck1',
			disabled: false,
			switch: false,
			binary: false,
			value: 1
		}}
		argTypes={{
			id: {
				description: 'Input element id',
				table: { category: 'Attributes' }
			},
			name: {
				description: 'Input element name',
				table: { category: 'Attributes' }
			},
			value: {
				description: 'Input element',
				table: { category: 'Attributes' }
			}
		}}>
		{Template.bind({})}
	</Story>
</Canvas>

<ArgsTable story="vv-checkbox" />
