import { Canvas, Meta, Story } from '@storybook/addon-docs'
import VvTextarea from '@/components/VvTextarea/VvTextarea.vue'
import { Template } from './Textarea.stories.mdx'
import { defaultTest } from './Textarea.test.ts'
import { defaultArgs, argTypes } from './Textarea.settings'

<Meta
	title="Components/Textarea/Length"
	component={VvTextarea}
	args={defaultArgs}
	argTypes={argTypes}
/>

<Canvas>
	<Story
		name="Maxlength"
		play={defaultTest}
		args={{
			maxlength: 5
		}}>
		{Template.bind()}
	</Story>
</Canvas>

<Canvas>
	<Story
		name="Minlength"
		play={defaultTest}
		args={{
			minlength: 5
		}}>
		{Template.bind()}
	</Story>
</Canvas>

<Canvas>
	<Story
		name="Count"
		play={defaultTest}
		args={{
			count: true,
			minlength: 100
		}}>
		{Template.bind()}
	</Story>
</Canvas>

<Canvas>
	<Story
		name="Limit"
		play={defaultTest}
		args={{
			count: 'limit',
			maxlength: 100
		}}>
		{Template.bind()}
	</Story>
</Canvas>

<Canvas>
	<Story
		name="Countdown"
		play={defaultTest}
		args={{
			count: 'countdown',
			maxlength: 100
		}}>
		{Template.bind()}
	</Story>
</Canvas>
