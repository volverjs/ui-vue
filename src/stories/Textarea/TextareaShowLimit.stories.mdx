import { Canvas, Meta, Story, Source, ArgsTable } from '@storybook/addon-docs'
import { action } from '@storybook/addon-actions'

import VvTextarea from '../../components/VvTextarea/VvTextarea.vue'
import { Template } from './Textarea.stories.mdx'

<Meta title="Components/Textarea/Props/ShowLimit" component={VvTextarea} />

# ShowLimit

`ShowLimit` mostra il contatore dei caratteri digitati.

<Canvas>
	<Story
		name="ShowLimit"
		args={{
			name: 'textarea-show-limit',
			showLimit: true
		}}>
		{Template.bind({})}
	</Story>
</Canvas>

Se imposto anche il maxlength, visualizza anche il numero massimo di caratteri che posso digitare

<Canvas>
	<Story
		name="ShowLimit with Max"
		args={{
			name: 'textarea-show-limit',
			showLimit: true,
			maxlength: 25
		}}>
		{Template.bind({})}
	</Story>
</Canvas>
