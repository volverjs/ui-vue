import { Canvas, Meta, Story, Source, ArgsTable } from '@storybook/addon-docs'
import { action } from '@storybook/addon-actions'

import VvTextarea from '../../components/VvTextarea/VvTextarea.vue'
import { Template } from './Textarea.stories.mdx'

<Meta title="Components/Textarea/Props/ShowLimitMode" component={VvTextarea} />

# ShowLimitMode

`ShowLimitMode='limit'` conteggia i caratteri inseriti.

<Canvas>
	<Story
		name="ShowLimit limit"
		args={{
			name: 'textarea-show-limit',
			showLimit: true,
			maxlength: 25
		}}>
		{Template.bind({})}
	</Story>
</Canvas>

`ShowLimitMode='countdown'` conteggia quanti caratteri posso ancora inserire.

<Canvas>
	<Story
		name="ShowLimit countdown"
		args={{
			name: 'textarea-show-limit',
			showLimit: true,
			maxlength: 25,
			showLimitMode: 'countdown'
		}}>
		{Template.bind({})}
	</Story>
</Canvas>
