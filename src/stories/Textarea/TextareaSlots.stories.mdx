import { Canvas, Meta, Story, Source, ArgsTable } from '@storybook/addon-docs'
import { action } from '@storybook/addon-actions'

import VvTextarea from '../../components/VvTextarea/VvTextarea.vue'
import VvIcon from '../../components/VvIcon/VvIcon.vue'
import { Template } from './Textarea.stories.mdx'
import { slotsTest } from './TextareaProps.test.ts'

<Meta title="Components/Textarea/Slots" component={VvTextarea} />

# Slot icons left e right

<Canvas>
	<Story name="left-right-slots" play={slotsTest}>
		{() => {
			return {
				components: { VvTextarea, VvIcon },
				data: () => ({ text: '' }),
				template: `
					<vv-textarea name="r1" v-model:modelValue="text" data-testid="textarea">
						<template v-slot:icon-left="slotProps">
                            <VvIcon name="add" data-testid="icon"/>
                        </template>
                        <template v-slot:icon-right>
                            <button data class="vv-button" data-testid="button"> QUA! </button>
                        </template>
					</vv-textarea>
                    `
			}
		}}
	</Story>
</Canvas>
