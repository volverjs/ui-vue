import { Canvas, Meta, Story, Source, ArgsTable } from '@storybook/addon-docs'
import { action } from '@storybook/addon-actions'
import VvTextarea from '@/components/VvTextarea/VvTextarea.vue'
import { Template } from './Textarea.stories.mdx'
import { hintLabelTest } from './Textarea.test'

<Meta title="Components/Textarea/Props/ValidLabel" component={VvTextarea} />

# ValidLabel

`ValidLabel` messaggio associato allo stato valid.

<Canvas>
	<Story
		name="ValidLabel"
		play={async (ctx) => {
			hintLabelTest(ctx, {
				className: 'vv-textarea--valid'
			})
		}}
		args={{
			name: 'textarea-validlabel',
			valid: true,
			validLabel: 'Valid label'
		}}>
		{Template.bind({})}
	</Story>
</Canvas>

<Canvas>
	<Story
		name="ValidLabels"
		play={async (ctx) => {
			hintLabelTest(ctx, {
				className: 'vv-textarea--valid'
			})
		}}
		args={{
			name: 'textarea-validlabels',
			valid: true,
			validLabel: ['Valid label 1', 'Valid label 2']
		}}>
		{Template.bind({})}
	</Story>
</Canvas>
