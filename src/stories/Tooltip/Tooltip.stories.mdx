import { Meta, Story, Canvas } from '@storybook/addon-docs'
import VvTooltip from '@/components/VvTooltip/VvTooltip.vue'
import VvButton from '@/components/VvButton/VvButton.vue'
import { defaultArgs, argTypes } from './Tooltip.settings'
import { defaultTest } from './Tooltip.test'

<Meta
	title="Components/Tooltip"
	component={VvTooltip}
	args={defaultArgs}
	argTypes={argTypes}
/>

export const Template = (args, { argTypes }) => ({
	props: Object.keys(argTypes),
	components: { VvTooltip, VvButton },
	setup() {
		return { args }
	},
	template: /* html */ `
		<vv-button class="fixed left-1/2 top-1/2 -translate-1/2" data-testId="parent">
			Hover me
			<vv-tooltip v-bind="args">
				<template #default v-if="args.default"><div v-html="args.default"></div></template>
			</vv-tooltip>
		</vv-button>
	`,
})

<Canvas>
	<Story name="Default" play={defaultTest}>
		{Template.bind()}
	</Story>
</Canvas>

<Canvas>
	<Story
		name="Top"
		play={defaultTest}
		args={{ position: 'top' }}
		argTypes={{
			position: {
				control: {
					disable: true,
				},
			},
		}}
	>
		{Template.bind({})}
	</Story>
</Canvas>

<Canvas>
	<Story
		name="Bottom"
		play={defaultTest}
		args={{ position: 'bottom' }}
		argTypes={{
			position: {
				control: {
					disable: true,
				},
			},
		}}
	>
		{Template.bind({})}
	</Story>
</Canvas>

<Canvas>
	<Story
		name="Right"
		play={defaultTest}
		args={{ position: 'right' }}
		argTypes={{
			position: {
				control: {
					disable: true,
				},
			},
		}}
	>
		{Template.bind({})}
	</Story>
</Canvas>

<Canvas>
	<Story
		name="Left"
		play={defaultTest}
		args={{ position: 'left' }}
		argTypes={{
			position: {
				control: {
					disable: true,
				},
			},
		}}
	>
		{Template.bind({})}
	</Story>
</Canvas>
