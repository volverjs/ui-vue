import { Meta, Story } from '@storybook/addon-docs'
import VvTooltip from '@/components/VvTooltip/VvTooltip.vue'
import { VvTooltipProps } from '@/components/VvTooltip/VvTooltip'
import ObjectUtilities from '@/utils/ObjectUtilities'

<Meta
	title="Components/Tooltip"
	component={VvTooltip}
	args={{ ...ObjectUtilities.propsToObject(VvTooltipProps) }}
	argTypes={{
		position: {
			type: 'select',
			options: [undefined, 'left', 'right', 'bottom', 'top']
		}
	}}
/>

export const Template = (args) => ({
	components: { VvTooltip },
	setup() {
		return { args }
	},
	template:
		/* html */
		`<div class="m-xl pl-xl">
			<VvTooltip v-bind="args">
				pippo
				<template #tooltip>
					Let's make our tooltip!
				</template>
			</VvTooltip>
		</div>`
})

<Story name="vv-tooltip" args={{}}>
	{Template.bind({})}
</Story>
