import { Meta, Story } from '@storybook/addon-docs'
import VvTooltip from '@/components/VvTooltip/VvTooltip.vue'
import { VvTooltipProps } from '@/components/VvTooltip/VvTooltip'
import ObjectUtilities from '@/utils/ObjectUtilities'
import { Template } from './Tooltip.stories.mdx'

<Meta title="Components/Tooltip/Props/position" component={VvTooltip} />

# VvTooltip position

Let's define a story for our `top` `position` tooltip.

<Story name="top" args={{ position: 'top' }}>
	{Template.bind({})}
</Story>

Let's define a story for our `bottom` `position` tooltip.

<Story name="bottom" args={{ position: 'bottom' }}>
	{Template.bind({})}
</Story>

Let's define a story for our `right` `position` tooltip.

<Story name="right" args={{ position: 'right' }}>
	{Template.bind({})}
</Story>

Let's define a story for our `left` `position` tooltip.

<Story name="left" args={{ position: 'left' }}>
	{Template.bind({})}
</Story>
