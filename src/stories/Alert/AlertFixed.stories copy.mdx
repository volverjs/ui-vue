import { Meta, Story } from '@storybook/addon-docs'
import VvAlert from '../../components/VvAlert/VvAlert.vue'

import { Template } from './Alert.stories.mdx'

<Meta
	title="Components/Alert/Props/Fixed"
	component={VvAlert}
	args={{ title: 'Alert variant', message: 'Alert message' }}
/>

# Fixed

Visualizza l'alert in posizione fixed (default: top centrale).
Il posizionamento avviene definendo lo `style` o utilizzando delle css class

<Story
	name="fixed"
	args={{ fixed: true, visible: true, class: 'inset-x-lg top-lg' }}>
	{Template.bind({})}
</Story>

<Story
	name="fixed-autoclose"
	args={{
		fixed: true,
		visible: true,
		closable: true,
		autoclose: 5000,
		class: 'inset-x-lg top-lg'
	}}>
	{Template.bind({})}
</Story>
