import { Canvas, Meta, Story, IconItem } from '@storybook/addon-docs'
import IconList from './IconList.vue'
import IconsNormal from '@/assets/icons/normal.json'

# Iconography

<Meta title="Components/Icon/iconography" component={IconList} />

export const Template = (args) => ({
	components: { IconList },
	setup() {
		return { args }
	},
	template: `<icon-list />`
})

<Story name="iconography" args={{}}>
	{Template.bind({})}
</Story>
