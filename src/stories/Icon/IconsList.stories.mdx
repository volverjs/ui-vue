import {
	Canvas,
	Meta,
	Story,
	IconGallery,
	IconItem
} from '@storybook/addon-docs'
import VvIcon from '../../components/VvIcon/VvIcon.vue'
import IconsNormal from '../../assets/icons/normal.json'

# Iconography

<Meta title="Components/Icon/list" component={VvIcon} />

export const Template = (args) => ({
	components: { VvIcon },
	setup() {
		console.log(args)
		return { args }
	},
	template:
		/*html*/
		`<vv-icon v-bind="args" name="menu" />`
})

<IconGallery>
	{Object.keys(IconsNormal.icons).map((key) => {
		return (
			<IconItem key={key} name={key}>
				{Template.bind({})}
			</IconItem>
		)
	})}
</IconGallery>
