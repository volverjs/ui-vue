import { Canvas, Meta, Story, Source } from '@storybook/addon-docs'
import VvIcon from '../../components/VvIcon/VvIcon.vue'
import { iconTest, classTest } from './IconTest.js'

<Meta title="Components/Icon/Props/prefix" />

# VvIcon prefix

Stories of `VvIcon` `prefix`.
By default prefix generated are: `simple`, `normal`, `detailed`

## Prefix normal (default)

Let's define a story for our `normal` `prefix` icon.

<Canvas>
	<Story
		name="normal"
		play={(ctx) =>
			iconTest({
				...ctx,
				functions: [classTest],
				classNames: ['vv-icon', 'iconify', 'iconify--normal']
			})
		}>
		{() => {
			return {
				components: { VvIcon },
				template:
					/*html*/
					`<vv-icon name="add" data-testid="img" />`
			}
		}}
	</Story>
</Canvas>

## Prefix simple

Let's define a story for our `simple` `prefix` icon.

<Canvas>
	<Story
		name="simple"
		play={(ctx) =>
			iconTest({
				...ctx,
				functions: [classTest],
				classNames: ['vv-icon', 'iconify', 'iconify--simple']
			})
		}>
		{() => {
			return {
				components: { VvIcon },
				template:
					/*html*/
					`<vv-icon name="add" data-testid="img" prefix="simple" />`
			}
		}}
	</Story>
</Canvas>

## Prefix detailed

Let's define a story for our `detailed` `prefix` icon.

<Canvas>
	<Story
		name="detailed"
		play={(ctx) =>
			iconTest({
				...ctx,
				functions: [classTest],
				classNames: ['vv-icon', 'iconify', 'iconify--detailed']
			})
		}>
		{() => {
			return {
				components: { VvIcon },
				template:
					/*html*/
					`<vv-icon name="add" data-testid="img" prefix="detailed" />`
			}
		}}
	</Story>
</Canvas>
