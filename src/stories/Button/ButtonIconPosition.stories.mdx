import { Canvas, Meta, Story, Source } from '@storybook/addon-docs'
import VvButton from '../../components/VvButton/VvButton.vue'
import { within, userEvent } from '@storybook/testing-library'
import { buttonTest, classTest } from './test.js'

<Meta title="Components/Button/Props/icon-position" />

# VvButton icon-position

Stories of `VvButton` with `icon`.

## Icon left (default)

Let's define a story for our text `icon` button.

<Canvas>
	<Story name="icon-left" play={buttonTest}>
		{() => {
			return {
				components: { VvButton },
				template:
					/*html*/
					`<vv-button label="Like" icon="heart" />`
			}
		}}
	</Story>
</Canvas>

<Source language="jsx" code={'<vv-button label="Icon left" icon="heart" />'} />

## Icon right

Let's define a story for our right `icon` button.

<Canvas>
	<Story
		name="icon-right"
		play={(ctx) =>
			buttonTest({
				...ctx,
				functions: [classTest],
				className: 'reverse'
			})
		}>
		{() => {
			return {
				components: { VvButton },
				template:
					/*html*/
					`<vv-button label="Icon right" icon="heart" icon-position="right" />`
			}
		}}
	</Story>
</Canvas>

<Source
	language="jsx"
	code={'<vv-button label="Icon right" icon="heart" icon-position="right" />'}
/>

## Icon top

Let's define a story for our top `icon` button.

<Canvas>
	<Story name="icon-top" play={buttonTest}>
		{() => {
			return {
				components: { VvButton },
				template:
					/*html*/
					`<vv-button label="Icon top" icon="heart" icon-position="top" />`
			}
		}}
	</Story>
</Canvas>

<Source
	language="jsx"
	code={'<vv-button label="Icon top" icon="heart" icon-position="top" />'}
/>

## Icon bottom

Let's define a story for our bottom `icon` button.

<Canvas>
	<Story
		name="icon-bottom"
		play={(ctx) =>
			buttonTest({
				...ctx,
				functions: [classTest],
				className: 'reverse'
			})
		}>
		{() => {
			return {
				components: { VvButton },
				template:
					/*html*/
					`<vv-button label="Icon bottom" icon="heart" icon-position="bottom" />`
			}
		}}
	</Story>
</Canvas>

<Source
	language="jsx"
	code={'<vv-button label="Icon top" icon="heart" icon-position="bottom" />'}
/>
