import { Canvas, Meta, Story, Source } from '@storybook/addon-docs'
import VvButton from '@/components/VvButton/VvButton.vue'
import { within, userEvent } from '@storybook/testing-library'
import { testButton } from './Button.test'

<Meta title="Components/Button/Props/icon-position" />

# VvButton icon-position

Stories of `VvButton` with `icon`.

## Icon left (default)

Let's define a story for our text `icon` button.

<Canvas>
	<Story name="icon-left" play={testButton}>
		{() => {
			return {
				components: { VvButton },
				template: `<vv-button label="Like" icon="heart" />`
			}
		}}
	</Story>
</Canvas>

<Source language="jsx" code={'<vv-button label="Icon left" icon="heart" />'} />

## Icon right

Let's define a story for our right `icon` button.

<Canvas>
	<Story
		name="icon-right"
		play={async (ctx) => {
			testButton(ctx, {
				className: 'vv-button--reverse'
			})
		}}>
		{() => {
			return {
				components: { VvButton },
				template: `<vv-button label="Icon right" icon="heart" icon-position="right" />`
			}
		}}
	</Story>
</Canvas>

<Source
	language="jsx"
	code={'<vv-button label="Icon right" icon="heart" icon-position="right" />'}
/>

## Icon top

Let's define a story for our top `icon` button.

<Canvas>
	<Story
		name="icon-top"
		play={async (ctx) => {
			testButton(ctx, {
				className: 'vv-button--column'
			})
		}}>
		{() => {
			return {
				components: { VvButton },
				template: `<vv-button label="Icon top" icon="heart" icon-position="top" />`
			}
		}}
	</Story>
</Canvas>

<Source
	language="jsx"
	code={'<vv-button label="Icon top" icon="heart" icon-position="top" />'}
/>

## Icon bottom

Let's define a story for our bottom `icon` button.

<Canvas>
	<Story
		name="icon-bottom"
		play={async (ctx) => {
			testButton(ctx, {
				className: ['vv-button--column', 'vv-button--reverse']
			})
		}}>
		{() => {
			return {
				components: { VvButton },
				template: `<vv-button label="Icon bottom" icon="heart" icon-position="bottom" />`
			}
		}}
	</Story>
</Canvas>

<Source
	language="jsx"
	code={'<vv-button label="Icon top" icon="heart" icon-position="bottom" />'}
/>
