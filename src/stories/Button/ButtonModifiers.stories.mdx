import { Canvas, Meta, Story, Source } from '@storybook/addon-docs'
import VvButton from '../../components/VvButton/VvButton.vue'
import { within, userEvent } from '@storybook/testing-library'
import { buttonTest, classTest, cssTest, disableTest } from './test.js'

<Meta title="Components/Button/modifiers" />

# VvButton modifiers

Stories of `VvButton` default `modifiers`.

## Block

Let's define a story for our `block` button.
Create block level button that span the full width of a parent.

<Canvas>
	<Story
		name="block"
		play={(ctx) =>
			buttonTest({
				...ctx,
				functions: [classTest, cssTest],
				className: 'block',
				cssString: 'display: block'
			})
		}>
		{() => {
			return {
				components: { VvButton },
				template:
					/*html*/
					`<vv-button label="Block" block />`
			}
		}}
	</Story>
</Canvas>

<Source language="jsx" code={'<vv-button label="Block" block />'} />

## Rounded

Let's define a story for our `rounded` button:

<Canvas>
	<Story
		name="rounded"
		play={(ctx) =>
			buttonTest({
				...ctx,
				functions: [classTest, cssTest],
				className: 'rounded',
				cssString: 'border-radius: 9999px'
			})
		}>
		{() => {
			return {
				components: { VvButton },
				template:
					/*html*/
					`<vv-button label="Rounded" rounded />`
			}
		}}
	</Story>
</Canvas>

<Source language="jsx" code={'<vv-button label="Rounded" rounded />'} />

## Full-bleed

Let's define a story for our `full-bleed` button:

<Canvas>
	<Story
		name="full-bleed"
		play={(ctx) =>
			buttonTest({
				...ctx,
				functions: [classTest],
				className: 'full-bleed'
			})
		}>
		{() => {
			return {
				components: { VvButton },
				template:
					/*html*/
					`<div class="flex gap-4">
						<vv-button label="Full bleed" full-bleed />
					</div>`
			}
		}}
	</Story>
</Canvas>

<Source
	language="jsx"
	code={`<div class="flex gap-4">
	<vv-button label="Full bleed" full-bleed />
</div>`}
/>

## Disabled

Let's define a story for our `disabled` button.

<Canvas>
	<Story
		name="disabled"
		play={(ctx) =>
			buttonTest({
				...ctx,
				functions: [cssTest, disableTest],
				cssString: 'cursor: not-allowed'
			})
		}>
		{() => {
			return {
				components: { VvButton },
				template:
					/*html*/
					`<vv-button label="Disabled" disabled />`
			}
		}}
	</Story>
</Canvas>

<Source language="jsx" code={'<vv-button label="Disabled" disabled />'} />

## Active

Let's define a story for our `active` button.

<Canvas>
	<Story
		name="active"
		play={(ctx) =>
			buttonTest({
				...ctx,
				functions: [classTest],
				className: 'active'
			})
		}>
		{() => {
			return {
				components: { VvButton },
				template:
					/*html*/
					`<vv-button label="Active" active />`
			}
		}}
	</Story>
</Canvas>

<Source language="jsx" code={'<vv-button label="Active" active />'} />
