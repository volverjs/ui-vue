import { Canvas, Meta, Story, Source } from '@storybook/addon-docs'
import VvButton from '@/components/VvButton/VvButton.vue'
import { testButton } from './Button.test'
import { Template } from './Button.stories.mdx'

<Meta title="Components/Button/Props/modifiers" />

# VvButton modifiers

Stories of `VvButton` `modifiers`.

## Default

Let's define a story for our `default` button:

<Canvas>
	<Story name="default" play={testButton} args={{ label: 'Default' }}>
		{Template.bind()}
	</Story>
</Canvas>

<Source language="jsx" code={'<vv-button label="Default" />'} />

## Primary

Let's define a story for our `primary` button:

<Canvas>
	<Story
		name="primary"
		play={async (ctx) => {
			testButton(ctx, {
				className: ['vv-button--primary']
			})
		}}
		args={{ label: 'Primary', modifiers: 'primary' }}>
		{Template.bind()}
	</Story>
</Canvas>

## Secondary

Let's define a story for our `secondary` button:

<Canvas>
	<Story
		name="secondary"
		play={async (ctx) => {
			testButton(ctx, {
				className: 'vv-button--secondary'
			})
		}}
		args={{ label: 'Secondary', modifiers: 'secondary' }}>
		{Template.bind()}
	</Story>
</Canvas>

## Danger

Let's define a story for our `danger` button:

<Canvas>
	<Story
		name="danger"
		play={async (ctx) => {
			testButton(ctx, {
				className: 'vv-button--danger'
			})
		}}
		args={{ label: 'Danger', modifiers: 'danger' }}>
		{Template.bind()}
	</Story>
</Canvas>

## Ghost

Let's define a story for our `ghost` button:

<Canvas>
	<Story
		name="ghost"
		play={async (ctx) => {
			testButton(ctx, {
				className: 'vv-button--ghost'
			})
		}}
		args={{ label: 'Ghost', modifiers: 'ghost' }}>
		{Template.bind()}
	</Story>
</Canvas>

## Text

Let's define a story for our `link` button:

<Canvas>
	<Story
		name="link"
		play={async (ctx) => {
			testButton(ctx, {
				className: 'vv-button--link'
			})
		}}
		args={{ label: 'Link', modifiers: 'link' }}>
		{Template.bind()}
	</Story>
</Canvas>

## Static Light

Let's define a story for our `static-light` button:

<Canvas>
	<Story
		name="static-light"
		play={async (ctx) => {
			testButton(ctx, {
				className: 'vv-button--static-light'
			})
		}}>
		{() => {
			return {
				components: { VvButton },
				backgrounds: {
					default: 'dark'
				},
				template: `
					<div class="bg-black p-16">
						<vv-button label="Static Light" modifiers="static-light" />
					</div>
					`
			}
		}}
	</Story>
</Canvas>

## Static Dark

Let's define a story for our `static-dark` button:

<Canvas>
	<Story
		name="static-dark"
		play={async (ctx) => {
			testButton(ctx, {
				className: 'vv-button--static-dark'
			})
		}}
		args={{ label: 'Static Dark', modifiers: 'static-dark' }}>
		{Template.bind()}
	</Story>
</Canvas>

## Block

Let's define a story for our `block` button:

<Canvas>
	<Story
		name="block"
		play={async (ctx) => {
			testButton(ctx, {
				className: 'vv-button--block'
			})
		}}
		args={{ label: 'Block', modifiers: 'block' }}>
		{Template.bind()}
	</Story>
</Canvas>

## Rounded

Let's define a story for our `rounded` button:

<Canvas>
	<Story
		name="rounded"
		play={async (ctx) => {
			testButton(ctx, {
				className: 'vv-button--rounded'
			})
		}}
		args={{ label: 'Rounded', modifiers: 'rounded' }}>
		{Template.bind()}
	</Story>
</Canvas>

## Full-bleed

Let's define a story for our `full-bleed` button:

<Canvas>
	<Story
		name="full-bleed"
		play={async (ctx) => {
			testButton(ctx, {
				className: 'vv-button--full-bleed'
			})
		}}
		args={{ label: 'Full-bleed', modifiers: 'full-bleed' }}>
		{Template.bind()}
	</Story>
</Canvas>

## Action

Let's define a story for our `action` button:

<Canvas>
	<Story
		name="action"
		play={async (ctx) => {
			testButton(ctx, {
				className: 'vv-button--action'
			})
		}}
		args={{ label: 'Action', modifiers: 'action' }}>
		{Template.bind()}
	</Story>
</Canvas>

## Action-quiet

Let's define a story for our `action-quiet` button:

<Canvas>
	<Story
		name="action-quiet"
		play={async (ctx) => {
			testButton(ctx, {
				className: 'vv-button--action-quiet'
			})
		}}
		args={{ label: 'Action-quiet', modifiers: 'action-quiet' }}>
		{Template.bind()}
	</Story>
</Canvas>
