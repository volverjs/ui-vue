import { Canvas, Meta, Story, Source } from '@storybook/addon-docs'
import VvButton from '../../components/VvButton/VvButton.vue'
import { buttonTest, classTest } from './test.js'

<Meta title="Components/Button/Props/variant" />

# VvButton variants

Stories of `VvButton` `variant`.

## Default

Let's define a story for our `default` button:

<Canvas>
	<Story name="default" play={buttonTest}>
		{() => {
			return {
				components: { VvButton },
				template:
					/*html*/
					`<vv-button label="Default" data-testid="button" />`
			}
		}}
	</Story>
</Canvas>

<Source language="jsx" code={'<vv-button label="Default" />'} />

## Primary

Let's define a story for our `primary` button:

<Canvas>
	<Story
		name="primary"
		play={(ctx) =>
			buttonTest({
				...ctx,
				functions: [classTest],
				className: 'primary'
			})
		}>
		{() => {
			return {
				components: { VvButton },
				template:
					/*html*/
					`<vv-button label="Primary" variant="primary" />`
			}
		}}
	</Story>
</Canvas>

## Secondary

Let's define a story for our `secondary` button:

<Canvas>
	<Story
		name="secondary"
		play={(ctx) =>
			buttonTest({
				...ctx,
				functions: [classTest],
				className: 'secondary'
			})
		}>
		{() => {
			return {
				components: { VvButton },
				template:
					/*html*/
					`<vv-button label="Secondary" variant="secondary" />`
			}
		}}
	</Story>
</Canvas>

## Danger

Let's define a story for our `danger` button:

<Canvas>
	<Story
		name="danger"
		play={(ctx) =>
			buttonTest({
				...ctx,
				functions: [classTest],
				className: 'danger'
			})
		}>
		{() => {
			return {
				components: { VvButton },
				template:
					/*html*/
					`<vv-button label="Danger" variant="danger" />`
			}
		}}
	</Story>
</Canvas>

## Ghost

Let's define a story for our `ghost` button:

<Canvas>
	<Story
		name="ghost"
		play={(ctx) =>
			buttonTest({
				...ctx,
				functions: [classTest],
				className: 'ghost'
			})
		}>
		{() => {
			return {
				components: { VvButton },
				template:
					/*html*/
					`<vv-button label="Ghost" variant="ghost" />`
			}
		}}
	</Story>
</Canvas>

## Text

Let's define a story for our `text` button:

<Canvas>
	<Story
		name="text"
		play={(ctx) =>
			buttonTest({
				...ctx,
				functions: [classTest],
				className: 'text'
			})
		}>
		{() => {
			return {
				components: { VvButton },
				template:
					/*html*/
					`<vv-button label="Text" variant="text" />`
			}
		}}
	</Story>
</Canvas>

## Static Light

Let's define a story for our `static-light` button:

<Canvas>
	<Story
		name="static-light"
		play={(ctx) =>
			buttonTest({
				...ctx,
				functions: [classTest],
				className: 'static-light'
			})
		}>
		{() => {
			return {
				components: { VvButton },
				backgrounds: {
					default: 'dark'
				},
				template:
					/*html*/
					`
					<div class="bg-black p-16">
						<vv-button label="Static Light" variant="static-light" />
					</div>
					`
			}
		}}
	</Story>
</Canvas>

## Static Dark

Let's define a story for our `static-dark` button:

<Canvas>
	<Story
		name="static-dark"
		play={(ctx) =>
			buttonTest({
				...ctx,
				functions: [classTest],
				className: 'static-dark'
			})
		}>
		{() => {
			return {
				components: { VvButton },
				template:
					/*html*/
					`<vv-button label="Static Dark" variant="static-dark" />`
			}
		}}
	</Story>
</Canvas>
