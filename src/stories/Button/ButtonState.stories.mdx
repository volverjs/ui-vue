import { Canvas, Meta, Story } from '@storybook/addon-docs'
import { Template } from './Button.stories.mdx'
import { defaultTest } from './Button.test'
import { argTypes, defaultArgs } from './Button.settings'

<Meta title="Components/Button/State" args={defaultArgs} argTypes={argTypes} />

<Canvas>
	<Story
		name="Active"
		args={{
			active: true,
			label: 'Active button'
		}}
		argTypes={{
			active: {
				control: {
					disable: true
				}
			}
		}}
		play={async (ctx) => {
			defaultTest(ctx, {
				className: 'vv-button--active'
			})
		}}>
		{Template.bind()}
	</Story>
</Canvas>

<Canvas>
	<Story
		name="Pressed"
		args={{
			pressed: true,
			label: 'Pressed button',
			modifiers: 'action-quiet'
		}}
		argTypes={{
			pressed: {
				control: {
					disable: true
				}
			}
		}}
		play={async (ctx) => {
			defaultTest(ctx, {
				className: 'vv-button--pressed'
			})
		}}>
		{Template.bind()}
	</Story>
</Canvas>

<Canvas>
	<Story
		name="Disabled"
		args={{
			disabled: true,
			label: 'Disabled button'
		}}
		argTypes={{
			disabled: {
				control: {
					disable: true
				}
			}
		}}
		play={defaultTest}>
		{Template.bind()}
	</Story>
</Canvas>
