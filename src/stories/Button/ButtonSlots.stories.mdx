import { Canvas, Meta, Story } from '@storybook/addon-docs'
import VvButton from '@/components/VvButton/VvButton.vue'
import VvBadge from '@/components/VvBadge/VvBadge.vue'
import { Template } from './Button.stories.mdx'
import { defaultTest } from './Button.test'
import { argTypes, defaultArgs } from './Button.settings'

<Meta title="Components/Button/Slots" args={defaultArgs} argTypes={argTypes} />

<Canvas>
	<Story
		name="Default"
		argTypes={{
			default: {
				control: {
					disable: true,
				},
			},
		}}
		args={{
			default: 'Default <em class="italic">slot!</em>',
		}}
	>
		{Template.bind()}
	</Story>
</Canvas>

<Canvas>
	<Story
		name="Before"
		play={defaultTest}
		args={{
			label: 'Before slot',
		}}
		argTypes={{
			before: {
				control: {
					disable: true,
				},
			},
		}}
	>
		{(args) => {
			return {
				setup() {
					return { args }
				},
				components: { VvButton, VvBadge },
				template: /*html*/ `
					<vv-button v-bind="args" data-testId="element">
						<template #before>
							<vv-badge value="12" modifiers="danger" />
						</template>
					</vv-button>`,
			}
		}}
	</Story>
</Canvas>

<Canvas>
	<Story
		name="After"
		play={defaultTest}
		args={{
			label: 'After slot',
		}}
		argTypes={{
			after: {
				control: {
					disable: true,
				},
			},
		}}
	>
		{(args) => {
			return {
				setup() {
					return { args }
				},
				components: { VvButton, VvBadge },
				template: /*html*/ `
					<vv-button v-bind="args" data-testId="element">
						<template #after>
							<vv-badge value="12" modifiers="danger" />
						</template>
					</vv-button>
				`,
			}
		}}
	</Story>
</Canvas>

<Canvas>
	<Story
		name="Label"
		args={{
			icon: 'add',
		}}
	>
		{(args) => {
			return {
				components: { VvButton },
				setup() {
					return { args }
				},
				template: /*html*/ `
					<vv-button v-bind="args" data-testId="element">
						<template #label>
							Label <em class="italic">slot!</em>
						</template>
					</vv-button>
				`,
			}
		}}
	</Story>
</Canvas>
