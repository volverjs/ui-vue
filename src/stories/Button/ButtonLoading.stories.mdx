import { Canvas, Meta, Story, Source } from '@storybook/addon-docs'
import VvButton from '../../components/VvButton/VvButton.vue'
import { testButton } from './Button.test'

<Meta title="Components/Button/Props/loading" />

# VvButton loading

Stories of `VvButton` with `loading`.

## Loading

Let's define a story for our `loading` button.

<Canvas>
	<Story
		name="loading"
		play={async (ctx) => {
			testButton(ctx, {
				isClickDisabled: true
			})
		}}>
		{() => {
			return {
				components: { VvButton },
				template:
					/*html*/
					`<vv-button label="Loading" loading />`
			}
		}}
	</Story>
</Canvas>

<Source language="jsx" code={'<vv-button loading />'} />

## Loading icon

Let's define a story for `loading-icon`

<Canvas>
	<Story
		name="loading-icon"
		play={async (ctx) => {
			testButton(ctx, {
				isClickDisabled: true
			})
		}}>
		{() => {
			return {
				components: { VvButton },
				template:
					/*html*/
					`<vv-button loading-icon="reload" loading />`
			}
		}}
	</Story>
</Canvas>

<Source language="jsx" code={'<vv-button loading-icon="reload" loading />'} />

## Loading label

Let's define a story for `loading-label`

<Canvas>
	<Story
		name="loading-label"
		play={async (ctx) => {
			testButton(ctx, {
				isClickDisabled: true
			})
		}}>
		{() => {
			return {
				components: { VvButton },
				template:
					/*html*/
					`<vv-button loading-label="Caricamento..." loading />`
			}
		}}
	</Story>
</Canvas>

<Source
	language="jsx"
	code={'<vv-button loading-label="Caricamento..." loading />'}
/>
