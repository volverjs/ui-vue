import { Canvas, Meta, Story } from '@storybook/addon-docs'
import VvButton from '../../components/Button/VvButton.vue'
import { within, userEvent } from '@storybook/testing-library'

<Meta title="Components/Button/icon" />

# VvButton with icon

Stories of `VvButton` `icon`.

## Icon right (default)

Let's define a story for our button with right (default) `icon`:

<Canvas>
	<Story name="icon">
		{() => {
			return {
				components: { VvButton },
				template:
					/*html*/
					`<vv-button variant="secondary" iconPosition="left" icon="https://cdn.worldvectorlogo.com/logos/vue-js-1.svg" >Icon</vv-button>`
			}
		}}
	</Story>
</Canvas>
