import { Canvas, Meta, Story } from '@storybook/addon-docs'
import { defaultTest } from './Button.test'
import { Template } from './Button.stories.mdx'
import { argTypes, defaultArgs } from './Button.settings'

<Meta title="Components/Button/Icon" args={defaultArgs} argTypes={argTypes} />

<Canvas>
	<Story
		name="Top"
		args={{
			icon: 'add',
			iconPosition: 'top',
			label: 'Button with top icon',
			modifiers: 'ghost'
		}}
		argTypes={{
			iconPosition: {
				...argTypes.iconPosition,
				control: {
					disable: true
				}
			},
			icon: {
				...argTypes.icon,
				control: {
					disable: true
				}
			}
		}}
		play={defaultTest}>
		{Template.bind()}
	</Story>
</Canvas>

<Canvas>
	<Story
		name="Right"
		args={{
			icon: 'add',
			iconPosition: 'right',
			label: 'Button with right icon'
		}}
		argTypes={{
			iconPosition: {
				...argTypes.iconPosition,
				control: {
					disable: true
				}
			},
			icon: {
				...argTypes.icon,
				control: {
					disable: true
				}
			}
		}}
		play={defaultTest}>
		{Template.bind()}
	</Story>
</Canvas>

<Canvas>
	<Story
		name="Bottom"
		args={{
			icon: 'add',
			iconPosition: 'bottom',
			label: 'Button with bottom icon',
			modifiers: 'ghost'
		}}
		argTypes={{
			iconPosition: {
				...argTypes.iconPosition,
				control: {
					disable: true
				}
			},
			icon: {
				...argTypes.icon,
				control: {
					disable: true
				}
			}
		}}
		play={defaultTest}>
		{Template.bind()}
	</Story>
</Canvas>

<Canvas>
	<Story
		name="Left"
		args={{
			icon: 'add',
			label: 'Button with left icon'
		}}
		argTypes={{
			iconPosition: {
				...argTypes.iconPosition,
				control: {
					disable: true
				}
			},
			icon: {
				...argTypes.icon,
				control: {
					disable: true
				}
			}
		}}
		play={defaultTest}>
		{Template.bind()}
	</Story>
</Canvas>

<Canvas>
	<Story
		name="No label"
		args={{
			icon: 'add',
			label: '',
			ariaLabel: 'Add new item',
			modifiers: 'action-quiet'
		}}
		argTypes={{
			iconPosition: {
				...argTypes.iconPosition,
				control: {
					disable: true
				}
			},
			icon: {
				...argTypes.icon,
				control: {
					disable: true
				}
			},
			label: {
				control: {
					disable: true
				}
			}
		}}
		play={defaultTest}>
		{Template.bind()}
	</Story>
</Canvas>
