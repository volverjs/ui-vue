import { Canvas, Meta, Story, Source } from '@storybook/addon-docs'
import VvButton from '@/components/VvButton/VvButton.vue'
import { testButtonWithIconLeft, testButtonWithIconOnly } from './Button.test'

<Meta title="Components/Button/Props/icon" />

# VvButton icon

Stories of `VvButton` with `icon`.

## Icon and text

Let's define a story for our text `icon` button.

<Canvas>
	<Story name="icon-left" play={testButtonWithIconLeft}>
		{() => {
			return {
				components: { VvButton },
				template: `<vv-button label="Like" icon="heart" />`
			}
		}}
	</Story>
</Canvas>

<Source language="jsx" code={'<vv-button label="Icon left" icon="heart" />'} />

## Icon only

Let's define a story for our `icon` only button.

<Canvas>
	<Story name="icon-only" play={testButtonWithIconOnly}>
		{() => {
			return {
				components: { VvButton },
				template: `<vv-button icon="heart" aria-label="Add to favorites" rounded />`
			}
		}}
	</Story>
</Canvas>

<Source
	language="jsx"
	code={'<vv-button icon="heart" aria-label="Add to favorites" rounded />'}
/>
