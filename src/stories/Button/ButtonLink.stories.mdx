import { Canvas, Meta, Story, Source } from '@storybook/addon-docs'
import VvButton from '@/components/VvButton/VvButton.vue'
import { testButtonLink } from './Button.test'

<Meta title="Components/Button/Link" />

# VvButton link

Stories of `VvButton` with `link`.

## Button link text

<Canvas>
	<Story
		name="link-text"
		play={async (ctx) =>
			testButtonLink(ctx, {
				className: 'vv-button--text'
			})
		}>
		{() => {
			return {
				components: { VvButton },
				template: `<vv-button label="Link text" href="https://google.it" target="_blank" modifiers="text" @click="$event.preventDefault()" />`
			}
		}}
	</Story>
</Canvas>

<Source
	language="jsx"
	code={`<vv-button label="Link text" href="https://google.it" target="_blank" variant="text" />`}
/>

## Button link text disabled

<Canvas>
	<Story
		name="link-text-disabled"
		play={(ctx) =>
			testButtonLink(ctx, {
				isClickDisabled: true,
				className: 'vv-button--disabled'
			})
		}>
		{() => {
			return {
				components: { VvButton },
				template: `<vv-button label="Link text disabled" href="https://google.it" target="_blank" variant="text" disabled />`
			}
		}}
	</Story>
</Canvas>

<Source
	language="jsx"
	code={`<vv-button label="Link text disabled" href="https://google.it" target="_blank" variant="text" disabled />`}
/>

## Button link

<Canvas>
	<Story name="button-link" play={(ctx) => testButtonLink(ctx)}>
		{() => {
			return {
				components: { VvButton },
				template: `<vv-button label="Button link" href="https://google.it" target="_blank" icon="charm:link-external" icon-position="right" @click="$event.preventDefault()" />`
			}
		}}
	</Story>
</Canvas>

<Source
	language="jsx"
	code={`<vv-button
	label="Button link"
	href="https://google.it"
	target="_blank"
	icon="charm:link-external"
	icon-position="right"
/>`}
/>

## Button link disabled

<Canvas>
	<Story
		name="button-link-disabled"
		play={(ctx) =>
			testButtonLink(ctx, {
				isClickDisabled: true,
				className: 'vv-button--disabled'
			})
		}>
		{() => {
			return {
				components: { VvButton },
				template: `<vv-button label="Button link" href="https://google.it" target="_blank" disabled />`
			}
		}}
	</Story>
</Canvas>

<Source
	language="jsx"
	code={`<vv-button label="Button link" href="https://google.it" target="_blank" disabled />`}
/>

## Router-link

export const Template = () => ({
	components: { VvButton },
	template: `<vv-button label="Router link" to="/test" variant="text" />`
})

<Canvas>
	<Story name="router-link" args={{}} play={(ctx) => testButtonLink(ctx)}>
		{RouterLinkStory}
	</Story>
</Canvas>

<Source
	language="jsx"
	code={`<vv-button label="Router link" to="/" variant="text" />`}
/>
