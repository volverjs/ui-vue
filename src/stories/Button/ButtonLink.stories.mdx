import { Canvas, Meta, Story } from '@storybook/addon-docs'
import { defaultTest } from './Button.test'
import { Template } from './Button.stories.mdx'
import { argTypes, defaultArgs } from './Button.settings'

<Meta title="Components/Button/Link" args={defaultArgs} argTypes={argTypes} />

<Canvas>
	<Story
		name="Href"
		args={{
			icon: 'arrow-right',
			iconPosition: 'right',
			label: 'Link button',
			href: 'https://volverjs.github.io/style',
			target: '_blank',
		}}
		argTypes={{
			href: {
				...argTypes.href,
				control: {
					disable: true,
				},
			},
			target: {
				...argTypes.target,
				control: {
					disable: true,
				},
			},
		}}
		play={defaultTest}
	>
		{Template.bind()}
	</Story>
</Canvas>

<Canvas>
	<Story
		name="Href Disabled"
		args={{
			icon: 'arrow-right',
			iconPosition: 'right',
			label: 'Link button',
			href: 'https://volverjs.github.io/style',
			target: '_blank',
			disabled: true,
		}}
		argTypes={{
			href: {
				...argTypes.href,
				control: {
					disable: true,
				},
			},
			target: {
				...argTypes.target,
				control: {
					disable: true,
				},
			},
			disabled: {
				control: {
					disable: true,
				},
			},
		}}
		play={defaultTest}
	>
		{Template.bind()}
	</Story>
</Canvas>
