import { Meta, Story, Canvas } from '@storybook/addon-docs'
import VvAvatar from '@/components/VvAvatar/VvAvatar.vue'
import { defaultArgs, argTypes } from './Avatar.settings'
import { defaultTest } from './Avatar.test'

<Meta
	title="Components/Avatar"
	component={ VvAvatar }
	args={ defaultArgs }
	argTypes={ argTypes }
/>

export const Template = (args) => ({
	components: { VvAvatar },
	setup() {
		return { args }
	},
	template:
		/* html */`
		<div class="m-md">
			<vv-avatar v-bind="args" data-testId="element">
				<template v-if="args.default">{{ args.default }}</template>
				<template v-if="args.content">{{ args.content }}</template>
			</vv-avatar>
		</div>`
})

<Canvas>
	<Story name="Default" play={defaultTest}>
		{Template.bind()}
	</Story>
</Canvas>

<Canvas>
	<Story
		name="Image"
		play={defaultTest}
		args={{ imgSrc: 'https://i.pravatar.cc/300' }}
		argTypes={{
			imgSrc: {
				control: {
					disable: true,
				}
			}
		}}>
		{Template.bind({})}
	</Story>
</Canvas>

<Canvas>
	<Story
		name="Small"
		play={defaultTest}
		args={{ modifiers: '' }}>
		{Template.bind({})}
	</Story>
</Canvas>

<Canvas>
	<Story
		name="Medium"
		play={defaultTest}
		args={{ modifiers: 'md' }}>
		{Template.bind({})}
	</Story>
</Canvas>

<Canvas>
	<Story
		name="Large"
		play={defaultTest}
		args={{ modifiers: 'lg' }}>
		{Template.bind({})}
	</Story>
</Canvas>

<Canvas>
	<Story
		name="Rounded"
		play={defaultTest}
		args={{ modifiers: 'rounded', default: 'MR' }}>
		{Template.bind({})}
	</Story>
</Canvas>
