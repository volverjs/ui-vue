import { Canvas, Meta, Story } from '@storybook/addon-docs'
import ObjectUtilities from '../../utils/ObjectUtilities'
import VvBreadcrumb from '../../components/VvBreadcrumb/VvBreadcrumb.vue'
import { VvBreadcrumbProps } from '../../components/VvBreadcrumb/VvBreadcrumb'
import { breadcrumbTest } from './Breadcrumb.test.ts'

<Meta
	title="Components/Breadcrumb"
	component={VvBreadcrumb}
	args={{
		...ObjectUtilities.propsToObject(VvBreadcrumbProps),
		routes: [
			{
				label: 'Home',
				title: 'Home',
				href: '/home'
			},
			{
				label: 'Library',
				title: 'Library',
				href: '/library'
			},
			{
				label: 'Book 1',
				title: 'Book 1'
			}
		]
	}}
/>

export const Template = (args) => ({
	components: { VvBreadcrumb },
	setup() {
		return { args }
	},
	template: `<vv-breadcrumb v-bind="args" />`
})

# VvBreadcrumb

Stories of `VvBreadcrumb`.
Let's define a story for our playground `breadcrumb`:

<Canvas>
	<Story
		name="vv-breadcrumb"
		play={breadcrumbTest}
		args={{}}
		render={Template.bind()}
	/>
</Canvas>
