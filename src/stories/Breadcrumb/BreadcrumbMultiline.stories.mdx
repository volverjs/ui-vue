import { Canvas, Meta, Story } from '@storybook/addon-docs'
import { propsToObject } from '@/utils/ObjectUtilities'
import VvBreadcrumb from '@/components/VvBreadcrumb/VvBreadcrumb.vue'
import { VvBreadcrumbProps } from '@/components/VvBreadcrumb'
import { Template } from './Breadcrumb.stories.mdx'

<Meta
	title="Components/Breadcrumb/Props/multiline"
	component={VvBreadcrumb}
	args={{
		...propsToObject(VvBreadcrumbProps),
		multiline: true,
		routes: [
			{
				label: 'Home',
				title: 'Home',
				to: '/'
			},
			{
				label: 'Library',
				title: 'Library',
				to: '/library'
			},
			{
				label: 'Book 1',
				title: 'Book 1'
			}
		]
	}}
/>

# VvBreadcrumb

Stories of `VvBreadcrumb`.
Let's define a story for our `multiline` `breadcrumb`:

<Canvas>
	<Story name="multiline" args={{}}>
		{Template.bind()}
	</Story>
</Canvas>
