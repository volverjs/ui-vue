export enum ButtonIconPosition {
	left = 'left',
	right = 'right',
	top = 'top',
	bottom = 'bottom'
}

export enum ButtonTarget {
	blank = '_blank',
	self = '_self',
	parent = '_parent',
	top = '_top'
}

export enum ButtonTag {
	nuxtLink = 'nuxt-link',
	routerLink = 'router-link',
	a = 'a',
	button = 'button'
}
